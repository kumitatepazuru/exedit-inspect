# ぼかし (Blur) フィルタ リバースエンジニアリング解析

対象: `data/exedit.auf`(AviUtl 拡張編集の標準フィルタプラグイン)、32bit PE、
`ImageBase = 0x10000000`。以下のアドレスはすべて VA(`RVA = VA - 0x10000000`)。

## 再現方法

すべてのスクリプトは本プロジェクトの `main.py` を踏み台にして実行する。
`main.py` はコマンドラインで指定されたスクリプトに対して、DLLパス
(デフォルト `data/exedit.auf`)とヘッダー一覧(デフォルト `data/filter.h`)を
渡す役割を持つ。

```
uv run main.py inspect/blur/<script>.py
```

| スクリプト | 確認していること |
|---|---|
| `find_blur_addr.py` | `FILTER_DLL` 配列(先頭 `0x100a3e28`)をたどり、`ぼかし` の2つの登録エントリを抽出。トラックバー定義は完全に同一で、差は `サイズ固定` チェックボックスの有無だけ。両方とも `func_proc = 0x1000e2f0` を共有 |
| `decompile_blur.py` | `0x1000e2f0` 周辺に絞った `CFGFast` + angr デコンパイルで `func_proc` と **12個のワーカー** 全部を復元。角度の違う確認として、浮動小数点まみれでリフトできないカーブ変換ヘルパーは capstone で生ダンプ |
| `disasm_params.py` | `範囲`/`縦横比` → 半径の変換。マジックナンバー除算を定数解決した上で、半径が2分割されて **2回のボックスパス** になることを示す |
| `verify_radius_split.py` | 除算列を Python で再実装し、`範囲`×`縦横比` の全2,002,000通りが `trunc(x/1000)` と一致することを確認。さらに2パス合成後の実効カーネルの台が**ちょうど ±範囲**であることを全 r=0..1000 で確認 |
| `verify_mode_mapping.py` | `fp->flag & 0x20` の2分岐が触る構造体フィールドを読み、**24箇所のワーカー起動を機械的に抽出**して12個のワーカー表を復元。どれがキャンバスを広げるかも命令から判定 |
| `verify_pass_chain.py` | 4パスの順序・半径の受け渡し(グローバル4個)と、12ワーカーが**片方の軸のグローバルしか読まない**ことを走査で確認(=垂直/水平の割り当て確定) |
| `verify_box_average.py` | 1パスの中身。アルファ加重(プリマルチプライ往復)であること、そして**除数の違いだけが端処理の違いのすべて**であることを、除算箇所の抽出とPython再実装の数値比較で示す |
| `verify_canvas_growth.py` | `サイズ固定` と2種類のキャンバス拡張(通常経路 / 事前拡張経路)。最終サイズをPythonで再現 |
| `disasm_light_strength.py` | `光の強さ` の順変換/逆変換ヘルパー(`発光` の `拡散速度` と同一のもの)の全浮動小数点定数 |
| `verify_light_curve.py` | カーブ自体は誤差0で往復すること、平均が**べき平均**に変わること、`ぼかし` では **4096を超えようがない**こと(`発光` との決定的な違い)、色差が1/16に量子化されること |
| `simulate_blur_reference.py` | 上記すべてを織り込んだ整数忠実リファレンス実装。小さな合成画像に対して実行して結果を表示する |

`simulate_blur_reference.py` はパラメータを受け取る:

```
uv run main.py inspect/blur/simulate_blur_reference.py --range 8 --light 30
uv run main.py inspect/blur/simulate_blur_reference.py --aspect -600 --size-fixed
uv run main.py inspect/blur/simulate_blur_reference.py --frame --range 1000
```

## 1. 2つの登録エントリとモードの違い

`ぼかし` は `FILTER_DLL` として2回登録されており、どちらも
`func_proc = 0x1000e2f0` を共有する。`func_init` / `func_exit` / `func_update` /
`func_WndProc` はすべて NULL で、**このフィルタには UI 用のコードが一切無い**。
実行時の分岐は `fp->flag & 0x20` の1点のみで、画素フォーマットからして違う。

| | オブジェクトのフィルタ効果 | フレーム(映像)フィルタ |
|---|---|---|
| `FILTER_DLL` | `0x100a0720`、`flag=0x20` | `0x100a0848`、`flag=0x00` |
| チェックボックス | `サイズ固定` | なし |
| 画像バッファ | `*(fpip+0xAC)` | `fpip->ycp_edit` (`+4`) |
| 対バッファ | `*(fpip+0xB0)` | `fpip->ycp_temp` (`+8`) |
| 画素形式 | **8バイト/画素 PIXEL_YCA**(y, cb, cr, **a**) | 6バイト/画素 PIXEL_YC(アルファなし) |
| 行幅 | `*(fpip+0xEC)` [画素] | `fpip->max_w` (`+0x14`) [画素] |
| 幅・高さ | `*(fpip+0xB4)` / `*(fpip+0xB8)` | `fpip->w` (`+0xC`) / `fpip->h` (`+0x10`) |

行幅について: フレーム版のワーカーは `*(fpip+0x14)` を6倍して1行のバイト数に
している。`FILTER_PROC_INFO` のこの位置は `max_w`(バッファの確保幅)であり、
バイト数として文書化されている `line_size`(`+0x44`)ではない。exedit は
`line_size` を読んでいない。
| 半径の上限 | キャンバス最大サイズに収まるところまで | 各半径を独立に `寸法/2` でクランプ |
| サイズ変化 | あり(`サイズ固定` 次第) | なし |

なお `fp` は AviUtl の `FILTER` レイアウトそのものだが、これらは exedit が内部で
持つ効果テーブル(`0x100a3e28`)のエントリであり、AviUtl には登録されていない。
exedit は初期化時(`0x10031570` 付近)に自前で

* `fp+0x60` ← exedit 自身の `fp->exfunc`(AviUtl の `EXFUNC*`)
* `fp+0x64` ← **exedit 内部のヘルパーテーブル `0x100a41e0`**(SDK では `hwnd`)

を書き込んでいる。マルチスレッド起動は `[fp+0x60]+0xCC`
(`EXFUNC::exec_multi_thread_func`)、キャンバス拡張は `[fp+0x64]+0x44/+0x48`
という別系統で、この2つは混同しやすい。

## 2. パラメータ変換(`func_proc` 冒頭、フレームごとに1回)

トラックバーは `fp->track[]` の順に `範囲`(初期値5、範囲0~1000)、
`縦横比`(0、-1000~1000)、`光の強さ`(0、0~60)。

* **`範囲 == 0` は即 return**。画像には一切触れない。
* **半径**。`範囲` がそのまま両軸の半径になり、`縦横比` が**片方の軸だけ**を縮める:
  ```
  rx = ry = 範囲
  縦横比 > 0  ->  ry = trunc(範囲 * (1000 - 縦横比) / 1000)   // 縦が縮む = 横長のぼかし
  縦横比 < 0  ->  rx = trunc(範囲 * (1000 + 縦横比) / 1000)   // 横が縮む = 縦長のぼかし
  ```
  除算はコンパイラ生成のマジックナンバー除算(`0x10624dd3` を掛けて `>>38` 相当
  ＋符号補正)で、**四捨五入ではなく0方向への切り捨て**。到達しうる全
  2,002,000通りで `trunc(x/1000)` と一致することを確認済み
  (`verify_radius_split.py`)。`縦横比 = ±1000` でその軸の半径が0になり、
  完全な横ぼかし/縦ぼかしになる。
* **半径の2分割**。各軸の半径は直後に2つに割られる:
  ```
  r_hi = r - r/2 = ceil(r/2)     r_lo = r/2 = floor(r/2)
  ```
  この2つが別々のボックスパスとして走る(手順3)。

## 3. 4パスのチェーンと三角カーネル

固定で **4パス**、この順に走る(半径0のパスは丸ごとスキップされる):

```
pass 1  垂直    半径 ry_hi
pass 2  水平    半径 rx_hi
pass 3  垂直    半径 ry_lo
pass 4  水平    半径 rx_lo
```

各パスは「現画像を読む → 対バッファへ書く」で、`func_proc` が直後にポインタを
入れ替える(**ping-pong**)。スキップされたパスは入れ替えも飛ばすので連鎖は
壊れない。半径はグローバル経由でワーカーに渡る
(`MULTI_THREAD_FUNC` は引数を4個しか取れないため):

| グローバル | 中身 |
|---|---|
| `0x1011ec34` / `0x1011ec38` | 垂直 / 水平パスの半径 |
| `0x1011ec3c` / `0x1011ec40` | 垂直 / 水平パスのカーネル幅 = `半径*2+1` |

12個のワーカーがそれぞれ**片方の軸のグローバルしか読まない**ことを走査で
確認済み(`verify_pass_chain.py`)。

**1パスは一様重みのボックス平均**で、ガウシアン核はどこにも無い。ただし
同じ軸を2回通すので、合成カーネルは box(2*r_hi+1) ⊛ box(2*r_lo+1) =
**三角形(r が奇数なら上底3の台形)**になる。台の広がりは
`r_hi + r_lo = r` ちょうどで、つまり **`範囲` がそのまま実効半径**である。

| 範囲 | 2パスの半径 | 合成カーネル |
|---|---|---|
| 1 | 1, 0 | `[1,1,1]`(パス1本だけ、ただの3タップ box) |
| 2 | 1, 1 | `[1,2,3,2,1]` |
| 3 | 2, 1 | `[1,2,3,3,3,2,1]` |
| 4 | 2, 2 | `[1,2,3,4,5,4,3,2,1]` |

## 4. アルファ加重ボックス平均と、端の扱い

オブジェクト版の累積は単なる和ではなく、**プリマルチプライした加重和**:

```
各サンプルについて:
    a == 0     -> 何もしない(色にもアルファにも寄与しない)
    a < 4096   -> sum_y  += (y  * a) >> 12
                  sum_cb += (cb * a) >> 12
                  sum_cr += (cr * a) >> 12
                  sum_a  += a
    それ以外   -> sum_y += y;  sum_cb += cb;  sum_cr += cr;  sum_a += a

出力:
    sum_a != 0 -> out.y  = trunc(sum_y  * 4096.0 / sum_a)   // 色は「アルファの和」で割る
                  out.cb = trunc(sum_cb * 4096.0 / sum_a)
                  out.cr = trunc(sum_cr * 4096.0 / sum_a)
    sum_a == 0 -> 色は書き込まない(書き込み先の古い内容がそのまま残る)
    out.a = sum_a / 除数                                     // アルファは素の平均
```

**色をアルファの和で割る**のがポイントで、透明画素は色を黒へ引っ張らない。
だからぼかされたオブジェクトのふちは灰色にならず色味を保ったまま透明度だけが
落ちる。`sum_a == 0` のときに色を書かないのは一見危険だが、その画素の
アルファは必ず0になる(`0/除数`)ので、以降のパスでも合成でも読まれない。

そして**端の扱いの違いは、アルファを割る「除数」だけに現れる**
(`verify_box_average.py`):

| ワーカー系統 | 除数 | 意味 | 出力サイズ |
|---|---|---|---|
| オブジェクト・`サイズ固定` **オフ** | 常にカーネル幅 | 画像外は**透明として数える** → ふちがフェードする | 各軸 `+2*半径` |
| オブジェクト・`サイズ固定` **オン** | キャンバス内に残っているサンプル数 | 端で**正規化し直す** → ふちの不透明度が保たれる | 変わらず |
| フレーム | フレーム内に残っているサンプル数 | 同上(アルファが無いので輝度がそのまま) | 変わらず |

`サイズ固定` オフのワーカーだけが出力を `2*半径` 行(列)分多く書き、
`func_proc` が `*(fpip+0xB8)` / `*(fpip+0xB4)` にその分を足す。同時に出力が
半径分ずれるので、オブジェクトは広がったキャンバスの中央に収まる。

### 丸め誤差

`(c * a) >> 12` は算術シフトで切り捨てるため、半透明部分ではプリマルチプライの
往復で色がわずかにずれる。リファレンス実装で `範囲=2`・元色 `cb=-1000, cr=2000`
のスプライトを4パス通すと `cb ∈ [-1030, -1000]`、`cr ∈ [1980, 1998]` になる
(数値は `simulate_blur_reference.py` の出力)。1%未満なので実用上は問題ないが、
「ぼかしただけなのに色が完全には保存されない」ことの説明にはなる。

## 5. `サイズ固定` とキャンバス拡張

`サイズ固定` は**2回、別の意味で**読まれる。

1. **チェックボックスそのもの**(`fp->check[0]`)。オンならキャンバス拡張も
   半径のクランプも一切行わず、そのまま手順3へ進む。
2. **その場のローカルコピー**。どのワーカーを呼ぶかを決める。事前拡張経路
   (下記)はここだけを 1 に書き換える。

オフのときは、いずれかの半径がキャンバスの半分以上
(`2*rx_hi >= w` / `2*ry_hi >= h` / `2*rx_lo >= w` / `2*ry_lo >= h` のいずれか)
だと**事前拡張経路**に入る。スライディングウィンドウの中段ループが
`h - カーネル幅` 回なので、そのままでは回数が0以下になって破綻するためである。

```
table[0x48](B, 0, 0, w+2rx, h+2ry, ..., flags=2)       // 拡大サイズでバッファBをクリア
table[0x44](B, rx, ry, A, 0, 0, w, h, ..., 0x13000003) // Aを中央へ転送
swap A/B;  w += 2rx;  h += 2ry;  ローカルのサイズ固定 := 1
```

以後は非拡張ワーカーが拡大済みバッファ上で走るので、**最終サイズはどちらの
経路でも `(w+2rx, h+2ry)` で同じ**になる。

| w | h | 範囲 | 固定 | 半径 (rx, ry) | 事前拡張 | 最終キャンバス |
|---|---|---|---|---|---|---|
| 200 | 100 | 5 | 0 | (5, 5) | いいえ | 210 x 110 |
| 200 | 100 | 5 | 1 | (5, 5) | いいえ | 200 x 100 |
| 200 | 100 | 60 | 0 | (60, 60) | いいえ | 320 x 220 |
| 32 | 32 | 100 | 0 | (100, 100) | **はい** | 232 x 232 |
| 4000 | 100 | 100 | 0 | (48, 100) | **はい** | 4096 x 300 |

オブジェクト側で `範囲` に効く唯一の上限は最大キャンバスサイズで、
`w + 2*rx > 最大幅`(`0x10196748`)なら `rx = (最大幅 - w)/2` に切り詰められる
(高さは `0x101920e0`)。上の4000幅の例がこれ。

フレーム側にはキャンバスが無いので、代わりに**4つの半径を個別に `寸法/2` で
クランプ**する。個別なので実効的な総半径は最大 `2*(寸法/2) ≈ 寸法` まで届く。

## 6. `光の強さ` ― 指数/対数空間での平均

`光の強さ > 0` のときだけ、4パスループの**前後に1回ずつ**全画面変換が入る。
使われるヘルパーは `発光` の `拡散速度` とまったく同じもので、範囲が同じ 0~60
なのもそのためである。

* `base = 1.0 + clamp(光の強さ, 1, 100) * 0.001`(UIの1~60では 1.001~1.06)
* **ループ前**(オブジェクト: `0x10070220` → `0x10070290`)、画素を8バイトのまま
  `{float y; int8 cb; int8 cr; int16 a;}` へ組み替える:
  ```
  a <= 0        -> y = 0.0f, cb = 0, cr = 0   (アルファ自体は不変)
  それ以外      -> y  = (float) pow(base, clamp(y,0,4096)/16) - 1
                   cb = clamp((cb + 8) >> 4, -128, 127)
                   cr = clamp((cr + 8) >> 4, -128, 127)
  ```
* 4パスのぼかしはこの `curved` 値に対して実行される。輝度は float で加重平均、
  色差は符号付きバイトのまま平均される。
* **ループ後**(`0x100703f0` → `0x10070470`)に1回だけ逆変換:
  ```
  y  = round(ln(y + 1) * 16 / ln(base))    // クランプなしで16bit書き戻し
  cb = cb_byte << 4;   cr = cr_byte << 4
  ```

結果として輝度の平均が算術平均ではなく**底 `base` のべき平均**になる:

```
out = 16 * log_base( mean_i( base^(y_i / 16) ) )
```

`base` が1に近いほど算術平均に、大きいほど**窓内で最も明るいサンプルが支配的**に
なる。つまり `光の強さ` は「ぼかしても明るい所が明るいまま広がる」ための
パラメータで、何かを足したり掛けたりしているわけではない。

| 光の強さ | base | 窓 `[0,0,0,0,4096]` のぼかし後輝度 | 最大値に対する比 |
|---|---|---|---|
| 0 | (カーブを通らない) | 819 | 0.200 |
| 1 | 1.001 | 907 | 0.221 |
| 5 | 1.005 | 1337 | 0.326 |
| 10 | 1.010 | 1946 | 0.475 |
| 20 | 1.020 | 2816 | 0.688 |
| 30 | 1.030 | 3226 | 0.788 |
| 60 | 1.060 | 3654 | 0.892 |

### `発光` との決定的な違い

同じヘルパーを使っていても、**`発光` は6パスを curved 空間で「足す」のに対し、
`ぼかし` は「平均する」**。平均は入力の最小値と最大値の間に必ず収まるので、
逆変換の書き戻しにクランプが無くても **4096を超えることはあり得ない**
(750通りの窓で確認済み、`verify_light_curve.py`)。`発光` で問題になる
「満輝度超過→チャンネルごとのクリップ→色相のずれ」は `ぼかし` では起きない。

### 代償

* 全画面の往復変換が2パス増える(AviUtl の説明の「処理が重くなる」に対応)。
* 色差がぼかしの間ずっと**1/16に量子化**される。`(c+8)>>4` → `<<4` なので
  16単位のグリッドに乗り、-2048~2048 の範囲で 4097 段階が **256 段階**になる。
  さらに符号付きバイトの範囲が非対称なため、`+2048` は `+2032` に切られるが
  `-2048` はそのまま通る。

## 7. 関数マップ

| アドレス | 役割 |
|---|---|
| `0x1000e2f0` | `func_proc` ― パラメータ変換、モード分岐、キャンバス拡張、4パスループ×2系統、カーブの呼び出し |
| `0x1000eae0` / `0x1000eef0` | 整数・オブジェクト・`サイズ固定`オフ、垂直/水平(**キャンバスを広げる**) |
| `0x1000f310` / `0x1000f7e0` | 整数・オブジェクト・`サイズ固定`オン、垂直/水平 |
| `0x1000fcb0` / `0x1000ff40` | 整数・フレーム、垂直/水平 |
| `0x10010190` / `0x100105e0` | カーブ・オブジェクト・`サイズ固定`オフ、垂直/水平(**キャンバスを広げる**) |
| `0x10010a30` / `0x10010f20` | カーブ・オブジェクト・`サイズ固定`オン、垂直/水平 |
| `0x10011400` / `0x100116d0` | カーブ・フレーム、垂直/水平 |
| `0x10070220` → `0x10070290` | `光の強さ` 順変換(8バイト画素版) |
| `0x100703f0` → `0x10070470` | `光の強さ` 逆変換(8バイト画素版) |
| `0x10070550` / `0x10070700` | 同・6バイト画素版(`発光` の `拡散速度` と共有) |
| `0x10081f90` | exedit `table[0x48]` ― 矩形クリア(flag bit1 で8バイト/6バイトを切替) |
| `0x10081b40` | exedit `table[0x44]` ― 矩形転送 |
| `0x10031570` 付近 | exedit の効果テーブル初期化。`fp+0x60` に `EXFUNC*`、`fp+0x64` に `0x100a41e0` を書く |
| `0x10091ad8` | CRTヘルパー ― `double` → `int` 切り捨て変換(`fistp` 定型) |
| `0x10091c70` | `pow(double, double)` ラッパー |

グローバル:

| アドレス | 中身 |
|---|---|
| `0x1011ec34` / `0x1011ec38` | 垂直 / 水平パスの半径 |
| `0x1011ec3c` / `0x1011ec40` | 垂直 / 水平パスのカーネル幅 |
| `0x10196748` / `0x101920e0` | オブジェクトキャンバスの最大幅 / 最大高 |
| `0x10135c68` / `0x10149840` | カーブヘルパーが使う行幅[画素](オブジェクト8B / フレーム6B) |
| `0x101e42f4` / `0x101e4300` / `0x101e4304` | カーブヘルパーへの引数(画像 / w / h) |
| `0x101e4318` / `0x101e4310` | `base` / `1/ln(base)` |

## 8. 参照テーブル

`範囲` / `縦横比` → 半径と4パス:

| 範囲 | 縦横比 | rx | ry | pass1 V | pass2 H | pass3 V | pass4 H |
|---|---|---|---|---|---|---|---|
| 5 | 0 | 5 | 5 | 3 | 3 | 2 | 2 |
| 5 | 500 | 5 | 2 | 1 | 3 | 1 | 2 |
| 5 | -500 | 2 | 5 | 3 | 1 | 2 | 1 |
| 5 | 1000 | 5 | 0 | 0 | 3 | 0 | 2 |
| 5 | -1000 | 0 | 5 | 3 | 0 | 2 | 0 |
| 20 | 250 | 20 | 15 | 8 | 10 | 7 | 10 |
| 100 | -750 | 25 | 100 | 50 | 13 | 50 | 12 |
| 1000 | 0 | 1000 | 1000 | 500 | 500 | 500 | 500 |

## 9. 未確定事項

* **UI表示値と raw の対応**。3つのトラックバーについて、AviUtl上に表示される
  数値と生値の対応関係は `exedit.auf` からは確認できない。`ぼかし` は
  `func_WndProc` が NULL で UI に一切干渉していないため、数値表示は
  AviUtl本体(`aviutl.exe`)側の汎用トラックバー描画がすべてを担っている。
  `光の強さ` だけは raw の範囲(0~60)がUIの範囲と一致するため 1:1 とみなせる。
* **カーブヘルパーが使う行幅グローバル `0x10135c68` と、ぼかしワーカーが使う
  `*(fpip+0xEC)` が同値であること**は前提にしている。同じフレームの同じ
  バッファを両者が走査している以上そうでなければ整合しないが、`0x10135c68` へ
  書き込む箇所自体は特定できていない。
* **`サイズ固定` オン＋`範囲` がキャンバスの半分以上**のとき。この組み合わせ
  だけは半径がクランプも事前拡張もされないため、ワーカーの位相ごとの固定
  ループ回数が画像をはみ出す ―― 画像外の読み出しと、中段位相が回らないことに
  よる**書き込まれない帯**(書き込み先バッファの古い内容が残る)が生じる。
  最大サイズで確保されたバッファ内なのでクラッシュはしないが、
  出力がどうなるかは確保時の内容次第なので `simulate_blur_reference.py` でも
  再現していない(該当セルは `-` と表示される)。
* フレーム側で寸法が偶数のとき、末尾位相が可視領域の1行(列)先まで書く。
  ループ境界からの導出で、`寸法/2` へのクランプ後 `カーネル幅 = 寸法+1` に
  なることによる。バッファ内なので実害は無い。
* **exedit `table[0x44]` / `table[0x48]` のフラグ語**(`0x13000003` と `2`)の
  完全な意味。bit1 が「8バイト画素(オブジェクト)」を選ぶことは確認したが、
  残りのビットは未解析。
