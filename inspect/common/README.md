# アドレス図鑑 / 共通実装

`data/exedit.auf`(32bit PE、`ImageBase = 0x10000000`)のうち、本プロジェクトで
**同定済みのアドレスを全部1箇所に集めた索引**。アドレスはすべて VA
(`RVA = VA - 0x10000000`)。

各行の「詳細」は根拠と検算スクリプトのある README を指す。まず概要でアタリを
付けて、必要なときだけ詳細へ飛ぶための表である。粒度は**関数・テーブル・
グローバル単位**で、「この1命令がこう分岐する」といった命令レベルの引用は
各エフェクトの README 側にある。

未知のアドレスを引くときは:

```
uv run main.py tools.xrefs --addr 0x........      # 参照元とそのエフェクトを推定
uv run main.py tools.disasm --addr 0x........ --size 0x100
uv run main.py tools.decompile --addr 0x........ --calls
```

## 共通実装ドキュメント

エフェクトをまたいで同じ実装が使われている部分は、個別 README から切り出して
以下にまとめてある。**新しいエフェクトを解析するときは、まずここを読めば
同じところを読み直さずに済む。**

- [`filter_registration.md`](filter_registration.md) ―― エフェクト登録構造体と
  ディスパッチ。`FILTER_DLL` 配列、オブジェクト効果 / フレームフィルタの2登録
  モデル(`fp->flag & 0x20`)、`FILTER_PROC_INFO` の exedit 独自オフセット、
  `fp+0x60`/`fp+0x64` の差し替えとマルチスレッド起動、`ex_data` のレイアウト。
- [`param_scaling.md`](param_scaling.md) ―― トラックバーの生値と UI 表示値の
  対応。`+0x74` 拡張ブロックの4スロット(表示スケール / グループ化マーカー /
  ドラッグ範囲)、全エフェクト分の一覧、マジックナンバー除算 `0x10624dd3`。
- [`box_blur.md`](box_blur.md) ―― アルファ加重(プリマルチプライ)平均と分離型
  ボックス平均。**ガウシアン核はどこにも無い**こと、色を `sum_a` で割る意味、
  除数の変種がそのまま端の扱いの違いであること(`グロー` は割らずに足し、
  `縁取り` は割らずに飽和させる)、スライディングウィンドウの3フェーズと
  既知の overrun。
- [`blend_modes.md`](blend_modes.md) ―― 矩形転送 `table[0x44]` / 矩形クリア
  `table[0x48]` と合成モード語の分解。ブレンド関数テーブル `0x1009fbb0`、
  加算 / 通常 / 単純コピーの実際の式。
- [`canvas_growth.md`](canvas_growth.md) ―― キャンバス拡張の4方式と
  `サイズ固定`。ワーカーが枠外に書く / `table` で事前拡張する(`縁取り` は
  それを**最後に取り消す**)/ オフスクリーン描画 / 矩形を計算して置き直す、の
  使い分けと最大キャンバスサイズ。
- [`exp_log_curve.md`](exp_log_curve.md) ―― `ぼかし` の `光の強さ` と `発光` の
  `拡散速度` が共有する指数/対数空間の輝度カーブ。**平均するか加算するか**の
  1点だけで上界の有無が分かれること、色差が 1/16 に量子化される代償。
- [`rgb_ycbcr.md`](rgb_ycbcr.md) ―― `光色の設定` の保存形式(bit24)と
  RGB→YCbCr(BT.601 フルレンジ、Q14)。`発光` と `閃光` で使い方が正反対である
  こと、暗い光色で効果が消える条件。
- [`integer_semantics.md`](integer_semantics.md) ―― C / x86 の整数意味論と CRT
  ヘルパー。`idiv`(0方向切り捨て)と `sar`(floor)が同じ式に同居すること、
  **色差でだけ結果が食い違う**理由、x87 の double 除算。
- [`angle_vector.md`](angle_vector.md) ―― `角度` トラックバーから Q16 の方向
  ベクトルを作る定型。定数 `±pi/1800` と `±65536.0`、符号の2系統(`凸エッジ` は
  `角度=0` が真下、`斜めクリッピング` は真上、回る向きはどちらも時計回り)、
  `_ftol`(0方向)と `sar`(floor)が混ざることで**第4象限だけサンプルを1つ
  損する**こと、x87 と libm が食い違いうる30度倍数の角度。
- [`lut_tables.md`](lut_tables.md) ―― `0x1006c680`(= **`ノイズ` の `func_init`**、
  効果テーブル初期化ループが起動時に呼ぶ)が生成する三角関数テーブル3本。
  イージングテーブル `0x101dcf78` の生成式、逆引き参照が同じ配列の左右反転に
  すぎないこと、`斜めクリッピング` の「添字を表からはみ出させない切り上げ」。
- [`thread_split.md`](thread_split.md) ―― ワーカーが担当範囲を計算する定型と、
  そこに付く2つのガード。垂直パスだけ列分割になる理由、`寸法 < thread_num` で
  **パスが丸ごと走らなくなる**変種と、それを避けている `thread_num = 1` の
  差し替え。
- [`luma_undershoot.md`](luma_undershoot.md) ―― 輝度が負に落ちたときの後始末。
  0 でクランプせず**最後の 1/4 レンジで色差を抜いてから黒へ**落とす定型で、
  `シャープ` / `色調補正` / 減算合成が共有する。
- [`object_time.md`](object_time.md) ―― オブジェクトの時間軸。`fp+0x118`/`+0x11C`
  (中間点で分割されていても**全体**の開始・終了フレーム)、`fpip+0xA8` の
  絶対フレーム、`fpip->frame` との恒等式、1/100秒 ⇄ フレームの x87 定型と
  0方向切り捨て、`テキスト` だけが書く `fpip+0x118`(サブオブジェクトの遅延)。
- [`border_correction.md`](border_correction.md) ―― `境界補正` を持つ3つの
  抜き系エフェクトが共有するマット後処理。垂直/水平ボックス平均 → 元マップとの
  積 → 伸張定数 `A = 4096 - 4096/r` / `B = 4096 mod r`。**式は同じでも
  「何をぼかすか」が違うと結果の意味が変わる**こと(`カラーキー` が半透明を
  潰すのはこれ)。

---

## 1. 共有ルーチン(複数エフェクトから呼ばれるコード)

| アドレス                    | 役割                     | 概要                                                                                                        | 詳細                                          |
| --------------------------- | ------------------------ | ----------------------------------------------------------------------------------------------------------- | --------------------------------------------- |
| `0x10007df0`                | 通常合成                 | src over dst。ブレンド関数テーブル添字 `0x00`。`out_c = (c_s*a_s + c_d*a_d*(1-a_s)) / out_a`                | [blend_modes](blend_modes.md)                 |
| `0x1000ae90`                | 加算合成                 | 添字 `0x11`。`(1-a_s)` の減衰項が無い。クランプ無しで16bit折り返し                                          | [blend_modes](blend_modes.md)                 |
| `0x1000afd0`                | 単純コピー               | 添字 `0x13`。`movdqu`/`rep movsw` の特例経路                                                                | [blend_modes](blend_modes.md)                 |
| `0x10018080`                | 汎用コンボ `func_WndProc` | `0x1e1c` を受けて `ex_data[0] = lParam`。**5登録が共有**(`ルミナンスキー`/`ミラー`/`色ずれ`×2/`インターレース解除`) | [filter_registration](filter_registration.md) |
| `0x10031570` 付近           | 効果テーブル初期化       | 全エフェクト構造体の `+0x60` に `EXFUNC*`、`+0x64` に `0x100a41e0` を書く(書き込みは `0x100315a4`)。**同じ周回で各エントリの `func_init` も呼ぶ**(`0x100315e6`) | [filter_registration](filter_registration.md) |
| `0x1004a050`                | `WM_FILTER_COMMAND` 送信 | 呼び出し元(`0x10041453` ほか)が `wParam = (コントロール添字 << 16) \| ID` を組み立てて渡す                 | [filter_registration](filter_registration.md) |
| `0x1004a430`                | `table[0x4c]` コントロールの HWND | `(fp+0xE4, 種別, 添字)`。種別 5 = ボタン / 6 = コンボ / 7 = 静的テキスト                          | [filter_registration](filter_registration.md) |
| `0x1004a7e0`                | ex_data フィールド変更通知 | `(fp+0xE4, フィールド名文字列)`。色選択ボタンが確定後に呼ぶ。**中身は未読**                               | [filter_registration](filter_registration.md) |
| `0x1004b200`                | 別オブジェクトとして描画 | `シャドー` の `影を別オブジェクトで描画` が `(fp+0xE4, fpip, 0x10fffff)` で呼ぶ。`モーションブラー` は `fpip` の時計を巻き戻しながら繰り返し呼ぶ。**中身は未読** | [シャドー](../shadow/README.md) / [object_time](object_time.md) |
| `0x10047be0` / `0x10047e30` | オブジェクト設定の充填   | `func_proc` を呼ぶ直前に `fp+0x44`〜`+0x124` と `fpip` の時間フィールドを埋める2本のほぼ同一ルーチン       | [object_time](object_time.md)                 |
| `0x1004993e` / `0x10049fab` | `func_proc` のディスパッチ | 戻り値を `1` と比較し、違えばそのオブジェクトのエフェクト列を打ち切る                                    | [フェード](../fade/README.md)                 |
| `0x1008b010`                | `テキスト` の1文字描画   | `0x1008aa28` からコールバックとして渡される。`表示速度` から `fpip+0x118`(サブオブジェクトの遅延)を作る**唯一の場所** | [object_time](object_time.md)     |
| `0x1004c8d0`                | `table[0x38]` 画像ファイル読み込み | `(dst, path, &w, &h, 0, 0)`。`dst = NULL` でサイズのみ取得。`画像ファイル` / `シャドー` / `縁取り` が共有 | [シャドー](../shadow/README.md)               |
| `0x1004cc40`                | `table[0x3c]` 保存パスの解決 | `wsprintfA` で `0x101539e0` に組み立てて属性を調べる。**基準ディレクトリは未追跡**                     | [シャドー](../shadow/README.md)               |
| `0x1004d2a0`                | `table[0x6c]` 色選択ダイアログ | `"GET_COLOR"` を添えて `sub_10020800` を呼ぶ。フラグ引数 bit8 が「色指定なし」トグルの有無らしい      | [filter_registration](filter_registration.md) |
| `0x1004dac0`                | `table[0x80]`            | `sub_1008d150()` を呼んだ後 `sub_1008d290` へ末尾呼び出し。**戻り値はどこでも捨てられる、中身は未読**       | [filter_registration](filter_registration.md) |
| `0x10020900`                | モーダルダイアログのラッパ | 前後で2つのウィンドウを `EnableWindow` する。`シャドー` と `縁取り` のファイル選択だけが呼ぶ。**中身は未読** | [シャドー](../shadow/README.md)               |
| `0x10065270`                | 汎用 `FILTER+0x58`       | `SendMessageA(combo, CB_SETCURSEL, ex_data[0], 0)`。`ルミナンスキー` と `インターレース解除` が共有         | [filter_registration](filter_registration.md) |
| `0x1006c680`                | 共有 LUT 初期化          | `0x101daf68`/`0x101dcf78`/`0x101def80` を cos/sin/1-cos で生成。**実体は `ノイズ` の `func_init`** ―― 効果テーブル初期化ループ(`0x100315e6`)が起動時に呼ぶので `ノイズ` を使わなくても表は埋まる | [lut_tables](lut_tables.md)                   |
| `0x10041358`〜`0x10041500`  | 設定ウィンドウの通知     | `wParam = (通し番号<<16) \| 0x1e19/0x1e1a/0x1e1b/0x1e1c`(トラックバー / チェック / ボタン / コンボ)      | [filter_registration](filter_registration.md) |
| `0x1006f520`                | RGB→YCbCr                | → `0x1006f6e0` / `0x1006f5b0`。係数は BT.601 フルレンジ(Q14)                                                | [rgb_ycbcr](rgb_ycbcr.md)                     |
| `0x1006fed0`                | 光色の分解               | `ex_data` 下位24bit を R/G/B に分け `0x1006f520` を呼ぶ。bit24 に関係なく毎フレーム実行                     | [rgb_ycbcr](rgb_ycbcr.md)                     |
| `0x10070220` → `0x10070290` | 輝度カーブ順変換(8B)     | `y = pow(base, clamp(y,0,4096)/16) - 1`、色差は `(c+8)>>4`                                                  | [exp_log_curve](exp_log_curve.md)             |
| `0x100703f0` → `0x10070470` | 同・逆変換(8B)           | `y = round(ln(y+1)*16/ln(base))`、**クランプなし**で16bit書き戻し                                           | [exp_log_curve](exp_log_curve.md)             |
| `0x10070550` → `0x100705c0` | 輝度カーブ順変換(6B)     | `ぼかし` フレーム版と `発光` の `拡散速度` が共有                                                           | [exp_log_curve](exp_log_curve.md)             |
| `0x10070700` → `0x10070780` | 同・逆変換(6B)           | 同上                                                                                                        | [exp_log_curve](exp_log_curve.md)             |
| `0x10081b40`                | `table[0x44]` 矩形転送   | モード語の下位バイト=画素サイズ、上位バイト=合成関数の添字                                                  | [blend_modes](blend_modes.md)                 |
| `0x10081f90`                | `table[0x48]` 矩形クリア | フラグ bit1 で8バイト/6バイト画素を切替                                                                     | [blend_modes](blend_modes.md)                 |
| `0x1008a520`                | オフスクリーン描画       | `[0x100b8c60]` 経由。`発光` の `サイズ固定` オフで呼ばれる。**中身は未読**                                  | [canvas_growth](canvas_growth.md)             |
| `0x1004e190` / `0x1004e1d0` | ファイル名/拡張子の抽出   | `PathFindFileName`/`PathFindExtension` 相当(DBCS対応の手書き実装)。`ワイプ`/`シーンチェンジ` が共有        | [ワイプ](../wipe/README.md)                   |
| `0x1004e220` / `0x1004e270` | `FindFirstFileA`/`NextFileA` の簡易ラッパー | 共有 `WIN32_FIND_DATAA`(`0x101a56d8`)を使う「現在取得→次を先読み」形。同上                | [ワイプ](../wipe/README.md)                   |
| `0x10091ad8`                | CRT `_ftol`              | `double` → `int` の0方向切り捨て(`or ah,0xc` で RC=truncate にして `fistp`)。解析済み全エフェクトに出てくる | [integer_semantics](integer_semantics.md)     |
| `0x10091c70` → `0x10091c92` | `pow` ラッパー           | `pow(double, double)`                                                                                       | [integer_semantics](integer_semantics.md)     |

## 2. エフェクト固有のコード

### ぼかし ― [`inspect/blur`](../blur/README.md)

| アドレス                    | 役割                | 概要                                                                                |
| --------------------------- | ------------------- | ----------------------------------------------------------------------------------- |
| `0x1000e2f0`                | `func_proc`         | パラメータ変換、`flag&0x20` 分岐、キャンバス拡張、4パスループ×2系統、カーブ呼び出し |
| `0x1000eae0` / `0x1000eef0` | ワーカー(垂直/水平) | 整数・オブジェクト・`サイズ固定`オフ。**キャンバスを広げる**。除数は常にカーネル幅  |
| `0x1000f310` / `0x1000f7e0` | ワーカー(垂直/水平) | 整数・オブジェクト・`サイズ固定`オン。除数は生存サンプル数                          |
| `0x1000fcb0` / `0x1000ff40` | ワーカー(垂直/水平) | 整数・フレーム(6バイト/画素)                                                        |
| `0x10010190` / `0x100105e0` | ワーカー(垂直/水平) | カーブ・オブジェクト・`サイズ固定`オフ。**キャンバスを広げる**                      |
| `0x10010a30` / `0x10010f20` | ワーカー(垂直/水平) | カーブ・オブジェクト・`サイズ固定`オン                                              |
| `0x10011400` / `0x100116d0` | ワーカー(垂直/水平) | カーブ・フレーム                                                                    |

12ワーカーはそれぞれ**片方の軸のグローバルしか読まない**(垂直/水平の割り当ての
根拠)。`func_init`/`func_exit`/`func_update`/`func_WndProc` はすべて NULL。

### 境界ぼかし ― [`inspect/boundary_blur`](../boundary_blur/README.md)

| アドレス     | 役割                      | 概要                                                                                                          |
| ------------ | ------------------------- | ------------------------------------------------------------------------------------------------------------- |
| `0x10011b00` | `func_proc`               | 半径計算(`ぼかし` と同一式)、`w/2`・`h/2` への独立クランプ、チェックボックスによるワーカー選択                |
| `0x10011c30` | OFF: 縦ランプ             | 角丸めランプを対バッファ `*(fpip+0xB0)` の α へ書き込む。**偶数サイズ+半径=サイズ/2 でバッファ外へ1行溢れる** |
| `0x10011db0` | OFF: 横ランプ+合成        | 横ランプをその場で計算し、`dist = trunc(sqrt(v²+h²))`、`alpha' = max(alpha - 2*dist, 0)`                      |
| `0x10012060` | ON: 縦ボックス平均        | α チャンネルのみ、ゼロ埋め端処理                                                                              |
| `0x10012200` | ON: 横ボックス平均+再合成 | 末尾に非線形しきい値カーブ `alpha' = (a*t)>>12, t = ((a*blurred)>>11) - 4096`                                 |

**このフィルタが読み書きするのは α だけ**で、y/cb/cr は一切参照されない。
登録はオブジェクト効果1個のみ(フレーム版が存在しない)。

### 拡散光 ― [`inspect/diffusion_light`](../diffusion_light/README.md)

| アドレス     | 役割                    | 概要                                                                                               |
| ------------ | ----------------------- | -------------------------------------------------------------------------------------------------- |
| `0x1001c330` | `func_proc`             | `強さ` 変換、`拡散` の半径2分割(`r2 = trunc(拡散/2.236)`)、キャンバス準備、2ラウンドのディスパッチ |
| `0x1001c710` | 垂直(成長版)            | `*(fpip+0xAC)` → `*(fpip+0xB0)` を `2r+1` 列ずらして書く                                           |
| `0x1001cb10` | 水平+合成(成長版)       | `*(fpip+0xB0)` をイン・プレース処理し元画像と合成。除数は常にカーネル幅                            |
| `0x1001d710` | 垂直(`サイズ固定`)      | ずらし無し、除数は実サンプル数                                                                     |
| `0x1001dbd0` | 水平+合成(`サイズ固定`) | `*(fpip+0xB0)` を読んで `*(fpip+0xAC)` にイン・プレース合成                                        |
| `0x1001e4f0` | 垂直(フレーム)          | `ycp_edit` → `ycp_temp`、6バイト/画素                                                              |
| `0x1001e770` | 水平+合成(フレーム)     | `ycp_temp` → `ycp_edit` にイン・プレース。`強さ` は単純 lerp                                       |

合成は「**ぼかし結果の輝度が元より明るいときだけ書き込む**」。しきい値パラメータ
が1つも無いのはこのため。

### 閃光 ― [`inspect/glint`](../glint/README.md)

| アドレス     | 役割                     | 概要                                                                     |
| ------------ | ------------------------ | ------------------------------------------------------------------------ |
| `0x1004e560` | `func_proc`              | パラメータ変換、キャンバス拡張、ワーカー起動、最終描画、バッファ入れ替え |
| `0x1004e9c0` | 光線ワーカー(指定なし)   | ソース画素自身の色をサンプル。`sum += (y,cb,cr)*a >> 12`                 |
| `0x1004ec2e` | 出力エンコード(指定なし) | 平均・しきい値の減算・アンプリマルチプライ。両ワーカー共通の後半         |
| `0x1004ecbd` | 中心画素経路(指定なし)   | **未初期化のスタックスロットをアルファとして使う**(既知の不具合)         |
| `0x1004ee20` | 光線ワーカー(色指定)     | ソースのアルファだけをサンプルし指定色に掛ける                           |
| `0x1004f07c` | 出力エンコード(色指定)   | 同上                                                                     |
| `0x1004f109` | 中心画素経路(色指定)     | ソース画像を一切読まず、指定色に借り物アルファを掛けて書く               |
| `0x1004f260` | 未登録の `func_proc`     | フレームフィルタ版のスタブ。バッファを入れ替えて `return 1` するだけ     |
| `0x1004f3f0` | `func_WndProc`           | 色ダイアログと合成モードのコンボボックス                                 |
| `0x1004f4a0` | ラベル再構築             | ボタンラベル / コンボボックスの文字列を流し込む                          |
| `0x1004f560` | 設定ウィンドウ更新       | `FILTER+0x58`(exedit の流用)                                             |

`強さ` は掛け算ではなく**しきい値**(`T = 4096 - strength`)で、ワーカーは
`strength` を一度も読まない。出力は輝度をアルファに移す**アンプリマルチプライ**。

### 発光 ― [`inspect/luminous`](../luminous/README.md)

| アドレス                    | 役割                   | 概要                                                                           |
| --------------------------- | ---------------------- | ------------------------------------------------------------------------------ |
| `0x10053100`                | `func_proc`            | パラメータ変換、モード分岐、6パスループ、逆変換、合成の呼び出し                |
| `0x100533c0` / `0x100534b0` | 明部抽出(フレーム)     | bit24 セット / クリア                                                          |
| `0x100535a0` / `0x100536d0` | 明部抽出(オブジェクト) | 同上。**輝度だけがアルファ加重**、色差は素の値                                 |
| `0x10053800`                | 最終合成(フレーム)     | 3チャンネルの単純加算。`拡散速度>0` の時だけ呼ばれる                           |
| `0x10053890`                | 最終合成(オブジェクト) | **発光の輝度がそのままアルファになり、色はそれで割って正規化される**。常に実行 |
| `0x10053a30`                | パスのディスパッチャ   | 半径を方向別にクランプし、カーネル幅 `2r+1` を計算して2ステップを起動          |
| `0x10053b10`                | 垂直ボックス(通常精度) | `ycp_temp` → スクラッチ `0x101a5328`。半径は `h/2-1` でクランプ                |
| `0x10053da0`                | 垂直ボックス(縮小精度) | `拡散速度>0` 用                                                                |
| `0x100540a0` / `0x100544a0` | 水平ボックス(通常精度) | フレーム版 / オブジェクト版。スクラッチ → `ycp_temp` ＋**飽和付き蓄積**        |
| `0x100548a0`                | 水平ボックス(縮小精度) | 蓄積は float 加算＋色差バイトの毎パスクランプ                                  |
| `0x10054c90`                | `func_WndProc`         | `光色の設定` ボタンのラベル書き換えのみ                                        |

蓄積は単純加算ではなく、輝度 8192 で頭打ち＋**超えたら色差が白へ退色**する。

### グロー ― [`inspect/glow`](../glow/README.md)

| アドレス                    | 役割                       | 概要                                                                                          |
| --------------------------- | -------------------------- | ----------------------------------------------------------------------------------------------- |
| `0x10054db0`                | `func_proc`                | `強さ`/`拡散` の早期 return、しきい値変換、2段のキャンバス拡張、`形状` の6分岐、合成          |
| `0x10055540` / `0x10055690` | 明部抽出(オブジェクト)     | 指定なし = ソフトニー付きで**漸近的に恒等**、指定あり = 素のヒンジ `y - T` × 光色              |
| `0x100557a0` / `0x100558e0` | 明部抽出(フレーム)         | 同上。アルファ加重が無いだけ                                                                  |
| `0x10055a10`                | 垂直ボックス平均           | 蓄積 → スクラッチ。半径は `H` でクランプ                                                      |
| `0x10055c70`                | 水平ボックス平均           | スクラッチ → 蓄積。`ぼかし` 用。半径は `W` でクランプ                                         |
| `0x10055ed0`                | 水平ボックス**和** + 蓄積  | `通常` 用。**カーネル幅で割らず** `強さ` を掛けて足す。**半径をクランプしない**                |
| `0x10056220` / `0x100569e0` | ストリーク(縦 / 横)        | 半径 `r, r/2, r/4` × `強さ` `1, 2, 4` 倍の3スケール。利得は3つとも等しい                      |
| `0x100570d0` / `0x10057730` | ストリーク(斜め `\`)       | 左端起点 / 上端起点。ステップ `stride+1`                                                      |
| `0x10057d90` / `0x10058430` | ストリーク(斜め `/`)       | 右端起点 / 上端起点。ステップ `stride-1`                                                      |
| `0x10056680`                | 蓄積の終端処理             | 全ストリーク共通。輝度 8192 で頭打ち＋**色差は比を保って再スケール**                          |
| `0x10058aa0`                | 最終合成(オブジェクト)     | 発光の輝度がアルファになるアンプリマルチプライ + 元画像との3分岐。`光成分のみ` でスキップ    |
| `0x10058de0`                | 最終合成(フレーム)         | 16bit 加算のみ、クランプ無し                                                                  |
| `0x10058ea0`                | `func_WndProc`             | 色ダイアログと `形状` コンボボックス                                                          |

`強さ` は**唯一 `/1000` 変換を受けないパラメータ**。`拡散` は `rx`/`ry`/斜め半径の
3つになるが、斜め半径の `sqrt(rx²+ry²)` は必ず後続のクランプに潰されて
`min(rx,ry)` になる。`サイズ固定` が無いので、`拡散` は必ず外接矩形を変える。

### クロマキー ― [`inspect/chroma_key`](../chroma_key/README.md)

| アドレス     | 役割                        | 概要                                                                                     |
| ------------ | --------------------------- | ------------------------------------------------------------------------------------------ |
| `0x10012de0` | `func_proc`                 | `ex_data.status != 1` で即 return、グローバル5個、2×2の分岐。**画素ループを持たない**       |
| `0x10012f10` | ワーカー(`境界補正=0`)      | 色相角＋彩度の距離 `d` でアルファを掛けるだけ                                              |
| `0x100130b0` | 同・`色彩補正` オン         | 色差をキー色から遠ざけるアンミックス ＋ `透過補正`                                         |
| `0x10013340` / `0x10013500` | `境界補正` パス1 | `map B = min(d, 4096)`。`色彩補正` 版は色相成分だけの `map C` も書く                       |
| `0x100136e0` | `境界補正` パス2            | マットの垂直ボックス平均。**列でスレッド分割**。両経路で共通                               |
| `0x10013880` / `0x10013de0` | `境界補正` パス3 | 水平ボックス平均 → 元マットとの積 → 傾き `r` のコントラスト伸張 → アルファと色差へ適用   |
| `0x100143d0` | `func_WndProc`              | スポイトの起動(`0x1e1b`)と結果の受け取り(`0x1e1d`)だけ                                     |
| `0x100144c0` | 設定ウィンドウのラベル更新  | `ex_data.color_yc.y` を読む**唯一**の場所                                                  |
| `0x10014560` | `FILTER+0x58`               | 設定ウィンドウ更新エントリ(`閃光` の `0x1004f560` と同じ流用)                              |

**輝度を一切見ない**エフェクト(キー色側も画素側も Cb/Cr だけ)。彩度は
`max(|cb|,|cr|)` のチェビシェフノルム、色相は `atan2(cr,cb)` を 65536分割した
2進角で、差分は `int16` への切り詰めで巻き戻す。`/1000` のマジック除算も
表示スケールも1つも無い(`カラーキー` と共通の性質)。

### カラーキー ― [`inspect/color_key`](../color_key/README.md)

| アドレス     | 役割                                                                                    |
| ------------ | ----------------------------------------------------------------------------------------- |
| `0x100162c0` | `func_proc` ―― `ex_data.status != 1` で即 return、グローバル2個、4回のディスパッチ。**画素ループを持たない** |
| `0x10016340` | キー判定ワーカー。YCbCr の軸並行な直方体に入ったら `a = 0`。**2回ディスパッチされる**    |
| `0x10016430` | `境界補正` 垂直ボックス平均(アルファのみ)。**列でスレッド分割**                          |
| `0x100165d0` | 同・水平ボックス平均 → 元アルファとの積 → 傾き `r` のコントラスト伸張 → アルファへ適用 |
| `0x10016850` | `func_WndProc` ―― スポイトの起動(`0x1e1b`)と結果の受け取り(`0x1e1d`)だけ                |
| `0x10016940` | 設定ウィンドウのラベル更新。`クロマキー` と同じ3文字列を使う                            |
| `0x100169e0` | `FILTER+0x58`(`クロマキー` の `0x10014560` と同じ流用)                                   |

**浮動小数点が1命令も無い**唯一のエフェクト(x87 も `_ftol` もマジック除算も
0件)。距離を計算せず3軸を独立に閉区間判定するだけなので出力アルファは2値で、
ソフトエッジは `境界補正` からしか出てこない。書き込みは画素の `+6` だけで、
y/cb/cr は素通りする。

### ルミナンスキー ― [`inspect/luma_key`](../luma_key/README.md)

| アドレス     | 役割                                                                                    |
| ------------ | ----------------------------------------------------------------------------------------- |
| `0x10064ee0` | `func_proc` ―― `ex_data.type` の4分岐だけ。25命令。**画素にもトラックバーにも触らない**  |
| `0x10064f60` | ワーカー `暗い部分を透過`。`t = y - 基準輝度 + ぼかし`                                  |
| `0x10065020` | ワーカー `明るい部分を透過`。`t = 基準輝度 + ぼかし - y`。**上との差は1命令**            |
| `0x100650e0` | ワーカー `明暗部分を透過`。`t > ぼかし` なら `t = 2*ぼかし - t` に折り返す              |
| `0x100651b0` | ワーカー `明暗部分を透過(ぼかし無し)`。同じ折り返し + 符号判定のみ。**乗除算なし**       |

エフェクト固有コードは `0x10064ee0`〜`0x1006529e` の **959バイト**だけで、解析済み
最小。**グローバルが0個・浮動小数点0命令・共有ルーチンの呼び出し0件**で、
`func_WndProc`(`0x10018080`)と `FILTER+0x58`(`0x10065270`)は他エフェクトと共有の
汎用コードなので、自前の UI コードすら持たない。`y` しか読まず `a` しか書かず、
近傍も読まないので `境界補正` に相当するものも無い。
表示スケールも1つも無い唯一のエフェクト。

### エッジ抽出 ― [`inspect/edge_extraction`](../edge_extraction/README.md)

| アドレス     | 役割                     | 概要                                                                                               |
| ------------ | ------------------------ | -------------------------------------------------------------------------------------------------- |
| `0x10022d60` | `func_proc`              | パラメータ2本、光色分解、チェックボックス2個の3分岐、バッファ入れ替え。**画素ループを持たない**    |
| `0x10022e30` | ワーカー(色エッジ)      | 両チェックOFF。`y`/`cb`/`cr` に Prewitt、**平方根を3回**取って足す                                 |
| `0x100234c0` | ワーカー(輝度エッジ)    | `y * a` に Prewitt。既定                                                                           |
| `0x10023880` | ワーカー(透明度エッジ)  | `a` に Prewitt。プリマルチプライの分岐が無い                                                       |
| `0x10023af0` | `func_WndProc`           | `0x1e1a` でチェックボックスを**排他**にし、`0x1e1b` で色ダイアログ(フラグ `2`)                    |
| `0x10023c00` | ボタンラベル再構築       | bit24 を見ない。`RGB ( %d , %d , %d )` 固定                                                        |
| `0x10023c60` | `FILTER+0x58`            | 設定ウィンドウ更新エントリ(`閃光`/`クロマキー` と同じ流用)                                        |

3ワーカーとも **Prewitt オペレータ**(Sobel ではない)＋ `sqrt(Gx²+Gy²)`。出力の
y/cb/cr は光色そのもので、**エッジの強さがアルファになる**。外周1画素は必ず
アルファ0で潰される。キャンバス拡張も早期 return も無い。

### モザイク ― [`inspect/mozaic`](../mozaic/README.md)

| アドレス     | 役割        | 概要                                                                                             |
| ------------ | ----------- | ------------------------------------------------------------------------------------------------ |
| `0x1006b090` | `func_proc` | `サイズ<2` なら即 return、`flag&0x20` 分岐、`offset_x`/`offset_y` の計算、ワーカーのディスパッチ |
| `0x1006b180` | ワーカー    | オブジェクト + `タイル風` オフ(8バイト/画素、アルファ加重)                                       |
| `0x1006b470` | ワーカー    | フレーム + `タイル風` オフ(6バイト/画素、素の整数平均)                                           |
| `0x1006b6b0` | ワーカー    | オブジェクト + `タイル風` オン                                                                   |
| `0x1006ba40` | ワーカー    | フレーム + `タイル風` オン                                                                       |

**作業用バッファを一切使わない**(完全な in-place)。グリッドは
`offset = (dim/2) % size` なので**セル境界が必ず画像中心を通る**。
`タイル風` は平均の計算には一切影響せず、書き戻しループだけを変える。

### ライト ― [`inspect/light`](../light/README.md)

| アドレス     | 役割                          | 概要                                                                                          |
| ------------ | ----------------------------- | ----------------------------------------------------------------------------------------------- |
| `0x1005ba30` | `func_proc`                   | `強さ`/`比率` → 係数 A・B、`拡散` → 半径の2段クランプ、陰影パス起動、後光パス起動、最終合成    |
| `0x1005bcd0` / `0x1005be50` | 後光: 垂直/水平ボックス平均 | 素のアルファ→スクラッチ→アンプリマルチプライ・エンコード。**唯一キャンバスを広げる側** |
| `0x1005c080` / `0x1005c250` | 陰影(`逆光`オフ): 垂直/水平 | 素のアルファ、`+4096`バイアスあり。`fpip+0xAC` へ加算(合成ではない)                     |
| `0x1005c520` / `0x1005c700` | 陰影(`逆光`オン): 垂直/水平 | **反転**アルファ(`4096-a`)、バイアスなし                                                 |
| `0x1005c9c0` | `func_WndProc`                 | `色の設定` ボタンのみ。`table[0x6c]`/`table[0x80]` の実体を初めて突き止めた解析元          |
| `0x1005ca30` | ボタンラベル再構築             | `"RGB ( %d , %d , %d )"`                                                                     |
| `0x1005ca90` | `FILTER+0x58`                  | `0x1005ca30` を呼ぶだけ                                                                       |

`比率` が `強さ` 由来の係数を2つ(後光用 A・陰影用 B)に配分し、片方が0になる
極値(`-100.0%`/`100.0%`)がそれぞれ「陰影だけ」「後光だけ」の境界になる。
`陰影パス` は対バッファへの合成ではなく**オブジェクト自身の画素に加算**する
唯一の実装で、`後光パス` は同じボックス平均の型でも**キャンバスを実際に
広げる**側。`サイズ固定` に相当するチェックボックスはこのエフェクトに
存在しない(`グロー` と同じ性質)。

### シャドー ― [`inspect/shadow`](../shadow/README.md)

| アドレス     | 役割                        | 概要                                                                                         |
| ------------ | --------------------------- | ---------------------------------------------------------------------------------------------- |
| `0x10087fb0` | `func_proc`                 | `濃さ` の変換、`X`/`Y`/`拡散` のクランプ、キャンバス配置、パターン敷き詰め、4パス起動、合成 |
| `0x10088580` | パス1: 垂直(カーネルB)      | `fpip+0xAC` の**アルファだけ**を読んでスクラッチ プレーン1へ                                |
| `0x100886f0` | パス2: 水平(カーネルB)      | プレーン1 → プレーン2                                                                       |
| `0x10088840` | パス3: 垂直(カーネルA)      | プレーン2 → プレーン1                                                                       |
| `0x100889a0` | パス4: 水平(カーネルA)+ 出力 | 影色を Y/Cb/Cr へ**素のまま**、被覆率を α へ。除算は x87 の定数1個に畳み込み               |
| `0x10088bc0` | 同・パターン画像版          | 敷き詰め済みパターンの α に掛けるだけ。**色は書かない**                                     |
| `0x10088da0` | `func_WndProc`              | ボタン2個を `HIWORD(wParam)` で区別。`0x1e16` はファイル名の通知                            |
| `0x10088f40` | ボタンラベル再構築          | パスのファイル名部分 / `"RGB ( %d , %d , %d )"`                                             |
| `0x10089010` | `FILTER+0x58`               | `0x10088f40` を呼ぶだけ                                                                      |

軸ごとに2パスなので合成カーネルは**半幅ちょうど `拡散` の三角形**(`ぼかし` と
同じ分割)。オブジェクトの色は一切読まず、影は**シルエットだけの関数**である。
`ライト`/`閃光` と違って影色はアンプリマルチプライされないので、
**真っ黒な影(既定値)でも不透明度は落ちない**。

### 縁取り ― [`inspect/border`](../border/README.md)

| アドレス     | 役割                                                                                    |
| ------------ | ----------------------------------------------------------------------------------------- |
| `0x100515d0` | `func_proc` ―― `サイズ` の早期 return、死んだ `/1000`、事前パディング、`2*サイズ` の成長、パターン敷き詰め、2パス起動、合成、削り戻し |
| `0x10051ae0` | パス1: 垂直ボックス**和**(平均ではない)+ 利得 + 4096 飽和。α → 16bit スクラッチ        |
| `0x10051c80` | パス2: 水平ボックス和 + 縁色エンコード → `fpip+0xB0`                                     |
| `0x10051ea0` | 同・パターン画像版。**パターンのαに掛けるだけで色は書かない**                            |
| `0x10052070` | `func_WndProc`。ボタン2個を `HIWORD(wParam)` で区別(`シャドー` と同じ規約、添字だけ違う) |
| `0x10052200` | ボタンラベル再構築 / `0x100522d0` `FILTER+0x58`                                          |

**窓の和をカーネル幅で割らない**唯一のエフェクトで、画素ループに除算が1命令も
無い。そのため `ぼかし = 0` では一辺 `2*サイズ+1` の正方形による**膨張
(dilation)**そのものになり、合成カーネルは三角形ではなく**平らな箱**。
`ぼかし` は窓の和に掛かる利得 `round(1024/(0.02*サイズ*ぼかし+1))` で、
パス1・パス2の**両方**に掛かる。カーネルより小さいオブジェクトを先に
パディングして最後に削り戻すため、**スライディングウィンドウの枠外読みを
構造的に防いでいる解析済み唯一の実装**でもある。

### 凸エッジ ― [`inspect/convex_edge`](../convex_edge/README.md)

| アドレス     | 役割                                                                                                        |
| ------------ | ------------------------------------------------------------------------------------------------------------- |
| `0x10007a80` | `func_proc` ―― `幅` のクランプ、`角度` → Q16 方向ベクトル、描画品質の間引き、`高さ` → スケール、起動、入れ替え |
| `0x10007b90` | ワーカー ―― 行分割、アルファの方向差分の総和、輝度エンコード。**エフェクトの残り全部**                       |

`0x10007a80`〜`0x10007dcd` の **846バイト・2関数**で全部という、解析済み最小の
エフェクト(`ルミナンスキー` の959バイトを下回る)。**チェックボックスも
`ex_data` も `func_WndProc` も無い**ので UI コードが存在せず、直接呼び出しは
`_ftol` 1本だけ。アルファを高さマップとみなした面取りで、**アルファ自体は
書き換えない** ―― だから近傍を最大100画素読んでもキャンバスが動かない。
出力は暗側が乗算(色相を保って黒へ)、明側が加算(白飛びへ)で、この2つが
同居するのは解析済みでここだけ。

### シャープ ― [`inspect/sharp`](../sharp/README.md)

| アドレス     | 役割                                                                                                       |
| ------------ | ------------------------------------------------------------------------------------------------------------ |
| `0x10089030` | `func_proc` ―― 早期 return 2本、元画像をスクラッチへ複製、`範囲` の2分割、4パス+合成の起動。**画素ループを持たない** |
| `0x100891c0` | 垂直ボックス平均(列分割)。**`ぼかし` の `0x1000f310` と命令単位で同一**                                   |
| `0x10089690` | 水平ボックス平均(行分割)。同じく `ぼかし` の `0x1000f7e0` と同一                                          |
| `0x10089b60` | アンシャープ合成(行分割)。`強さ` の `/1000` はここ。**x87 命令0個の純整数コード**                         |

エフェクト固有コードは `0x10089030`〜`0x10089d99` の **3,434バイト・4関数**で、
うち2つは `ぼかし` からの丸ごと流用(差は分岐先とグローバル4本だけ)。
チェックボックスも `ex_data` も `func_WndProc` も無い。**元画像を8バイト画素の
まま `[0x101a5328]` へ複製する唯一のエフェクト**で、4パスの ping-pong で
消えてしまう引き算の相手をそこに退避している。合成はアルファを書き戻すだけで
変えず、半透明では差分が `4096/アルファ` 倍に増幅される。

### フェード ― [`inspect/fade`](../fade/README.md)

| アドレス     | 役割                                                                                                     |
| ------------ | ---------------------------------------------------------------------------------------------------------- |
| `0x1004dd40` | `func_proc` ―― 秒→フレーム変換(x87 + `_ftol`)、ランプ2本と min、3つの出口。**画素ループを持たない**    |
| `0x1004de20` | ワーカー ―― 行分割してアルファに `(a*g)>>12`。**エフェクトの残り全部**                                  |
| `0x1004dea0` | `音量フェード` の `func_proc`(別登録 `0x100a5810`)。変換部は**オペコード完全一致**、PCM に同じランプを掛ける |

`0x1004dd40`〜`0x1004de95` の **342バイト・2関数**で全部で、`凸エッジ` の846バイトを
下回る解析済み最小のエフェクト。**入力が画素ではなく時間**である初の解析対象で、
読むのは `fp+0x118`/`fp+0x11C`(オブジェクト全体の開始・終了フレーム)と
`fpip+0xA8`(現在フレーム)、フレームレート `fpip+0xFC`/`+0x100` だけ
([`object_time.md`](object_time.md))。書くのは 8バイト画素の `+6` = **アルファ
だけ**で、対バッファもスクラッチも使わない完全な in-place。アルファが 0 以下に
なると `func_proc` が **`return 0`** を返してオブジェクトごと描画を落とす
―― が、それが起きるのは `テキスト` の `文字毎に個別オブジェクト` が
`fpip+0x118` に遅延を入れたときだけである。

### ワイプ ― [`inspect/wipe`](../wipe/README.md)

| アドレス     | 役割                                                                                     |
| ------------ | ------------------------------------------------------------------------------------------ |
| `0x100903c0` | `func_init` ―― `transition\*.png` を列挙してコンボへ追記(`シーンチェンジ` と共有ヘルパー) |
| `0x10090490` | `func_proc` ―― `フェード` と同型の進捗ランプ+3出口 → パターン分岐 → ぼかし → 最終合成      |
| `0x10090a60`〜`0x10090f90` | 5種の組み込みパターン(時計・四角・円・横・縦)。`円` だけアンチエイリアス帯を持つ |
| `0x10090640`/`0x10090700`/`0x100908c0` | ぼかしのカーネル半径分割(`ぼかし` 本体と同じ `r_hi`/`r_lo` 分割)と2軸パス |
| `0x10091050` | 最終合成 ―― `objAlpha *= patternAlpha >> 12`(`フェード` の定数版のマスク版)              |
| `0x10091100`〜`0x10091500` | カスタムPNGパターン ―― 読み込み→拡縮→輝度しきい値化→3x3平均→合成、を自己完結で行う |
| `0x10091710` | `func_WndProc` ―― `ワイプ(円)` コンボ(固定5種+動的ファイル一覧)の選択適用             |

進捗ランプは `フェード` §4 の「2本のランプの min、3つの出口」と同型(`t` に
`fpip+0x118` が乗らない点だけ違う)だが、出した進捗をアルファへ直接掛けずに
**5種類の形状ワーカーのどれか1つ**([`box_blur.md`](box_blur.md) と同じ
`r_hi`/`r_lo` 分割のぼかしで境界を任意に軟化できる)でマスクを作ってから
オブジェクト自身のアルファに掛ける。`反転(イン)`/`反転(アウト)` チェックは
進捗の符号反転とパターンワーカー内の比較演算子入れ替えの**両方**に効く
一方の値で、円パターンだけ既定(未反転)での可視領域の伸び縮みの向きが
残り4パターンと逆 ―― 詳細と検算は `inspect/wipe/README.md` §4/§5。

カスタムPNGパターンが探索する `<exedit.aufのフォルダ>\transition\*.png` は
`シーンチェンジ`(`0x10086580` の `func_init`)とも共有されるインフラで、
`FindFirstFileA`/`FindNextFileA` の簡易ラッパー(`0x1004e220`/`0x1004e270`)と
ファイル名/拡張子抽出(`0x1004e190`/`0x1004e1d0`)も両エフェクトで共通。

### 斜めクリッピング ― [`inspect/diagonal_clipping`](../diagonal_clipping/README.md)

| アドレス     | 役割                                                                                        |
| ------------ | --------------------------------------------------------------------------------------------- |
| `0x1005cab0` | `func_proc` ―― トラックバー5本をグローバル6個に畳み、`幅 == 0` で2ワーカーに分岐。常に `return 1` |
| `0x1005cb70` | ワーカー(`幅 == 0`)―― 半平面クリップ。階調帯が直線の**上に centered** に乗る               |
| `0x1005ccb0` | ワーカー(`幅 != 0`)―― `幅 > 0` は帯を残し、`幅 < 0` は帯を消す。1関数に2変種が同居         |

エフェクト全体が `0x1005cab0`〜`0x1005cea7` の **1016バイト・3関数**で、
`_ftol` 以外の直接呼び出しも、共有ワーカーも、キャンバス操作も無い。各画素で

```
s = vx*(x - cx) + vy*(y - cy)          // Q16、|v| = 65536 が 1 画素
```

という符号付き距離を作り(行ごとに組み立てて列は `s += vx` の加算のみ)、
`幅` の3モードで `m` を決めてから、共通の末端でイージング表
([`lut_tables.md`](lut_tables.md))を引いて**アルファだけ**に掛ける。
`角度` → Q16 ベクトルは [`angle_vector.md`](angle_vector.md) の
`(+sinθ, -cosθ)` 系で、**`sin` 側が x** であることをワーカーの命令から
確定できた2例目(1例目は `凸エッジ`)。

## 3. 読み取り専用データ(`.rdata` / `.data`)

| アドレス                                   | 種別                 | 概要                                                                   | 詳細                                          |
| ------------------------------------------ | -------------------- | ---------------------------------------------------------------------- | --------------------------------------------- |
| `0x1009a3a0`                               | 定数                 | `4096.0`(プリマルチプライを戻すスケール)                               | [integer_semantics](integer_semantics.md)     |
| `0x1009a430`                               | 定数                 | `10430.378350470453` = `65536/(2π)`。ラジアン→16bit2進角              | [クロマキー](../chroma_key/README.md)         |
| `0x1009a538`                               | 定数                 | `100.0`。1/100秒 ⇄ フレームの換算。**26箇所が共有**                    | [object_time](object_time.md)                 |
| `0x1009a3e8` / `0x1009a408`                | 定数                 | `65536.0` / `-65536.0`。Q16 スケール(方向ベクトルほか)               | [angle_vector](angle_vector.md)               |
| `0x1009a410`                               | 定数                 | `+pi/1800`(1/10度→ラジアン)。**7エフェクトが共有**                   | [angle_vector](angle_vector.md)               |
| `0x1009a3f8`                               | 定数                 | 同・符号反転版。**`凸エッジ` の1箇所からしか参照されない**            | [凸エッジ](../convex_edge/README.md)          |
| `0x1009ec90` / `0x1009ec9c`                | `+0x74` 拡張ブロック | `凸エッジ` の表示スケール `{1,100,10}` / ブロック本体。**他3スロットは NULL** | [param_scaling](param_scaling.md)         |
| `0x1009ecb0`                               | `FILTER_DLL`         | `凸エッジ`(オブジェクトのみ)。`ex_data_size = 0`、コールバック全 NULL | [凸エッジ](../convex_edge/README.md)          |
| `0x100a0dd0`                               | `ex_data` フィールド表 | `クロマキー` の `+0x70`。`color_yc`(6) + 無名パディング(2) + `status`(4) | [クロマキー](../chroma_key/README.md)         |
| `0x100a0e28`                               | `FILTER_DLL`         | `クロマキー`(オブジェクトのみ)。`+0x74` は **NULL**                    | [クロマキー](../chroma_key/README.md)         |
| `0x100a0f5c` / `0x100a0f84`                | 文字列               | `キー色の取得` / `境界補正`。**`クロマキー` と `カラーキー` で共有**   | [カラーキー](../color_key/README.md)          |
| `0x100a0fbc` / `0x100a0fc8` / `0x100a0ff4` | 文字列               | `status` 0/2/1 に対応するボタンラベル。**同上、2エフェクトで共有**     | [クロマキー](../chroma_key/README.md)         |
| `0x100a16a0`                               | `ex_data` フィールド表 | `カラーキー` の `+0x70`。`0x100a0dd0` と**中身が完全に同じ別テーブル** | [カラーキー](../color_key/README.md)          |
| `0x100a16d8`                               | `FILTER_DLL`         | `カラーキー`(オブジェクトのみ)。`+0x74` は **NULL**                    | [カラーキー](../color_key/README.md)          |
| `0x1009fbb0`                               | 関数テーブル         | ブレンド関数(32エントリ)。`mode >> 24` が添字。読んだのは3エントリだけ | [blend_modes](blend_modes.md)                 |
| `0x100a0720` / `0x100a0848`                | `FILTER_DLL`         | `ぼかし` オブジェクト(`flag=0x20`)/ フレーム(`flag=0x00`)              | [ぼかし](../blur/README.md)                   |
| `0x100a09e0`                               | `FILTER_DLL`         | `境界ぼかし`(オブジェクトのみ)                                         | [境界ぼかし](../boundary_blur/README.md)      |
| `0x100a21b8`                               | `+0x74` 拡張ブロック | `拡散光`。2つの登録エントリで共有                                      | [param_scaling](param_scaling.md)             |
| `0x100a21d0` / `0x100a22f8`                | `FILTER_DLL`         | `拡散光` オブジェクト / フレーム                                       | [拡散光](../diffusion_light/README.md)        |
| `0x100a2b48` / `0x100a2b4c`                | `ex_data`            | `エッジ抽出` の `ex_data_def`(`0x00ffffff`)/ `+0x70` フィールド表     | [エッジ抽出](../edge_extraction/README.md)    |
| `0x100a2b84`                               | `+0x74` 拡張ブロック | `エッジ抽出`。表示スケール `{10, 100}` のみ、ドラッグ範囲は NULL      | [param_scaling](param_scaling.md)             |
| `0x100a2bb0`                               | `FILTER_DLL`         | `エッジ抽出`(オブジェクトのみ)                                        | [エッジ抽出](../edge_extraction/README.md)    |
| `0x100a2d24`                               | 文字列               | `RGB ( %d , %d , %d )`。`エッジ抽出` のボタンラベル書式               | [rgb_ycbcr](rgb_ycbcr.md)                     |
| `0x100a3e28`                               | 配列                 | **`FILTER_DLL*` 配列の先頭**。すべての解析の入口                       | [filter_registration](filter_registration.md) |
| `0x100a41e0`                               | 関数テーブル         | exedit 内部の描画関数テーブル。起動時に全構造体の `+0x64` へ書かれる   | [blend_modes](blend_modes.md)                 |
| `0x100a59f8`                               | `FILTER_DLL`         | `閃光`(オブジェクトのみ)                                               | [閃光](../glint/README.md)                    |
| `0x100a5b20`                               | `FILTER_DLL`         | **配列から到達できない**閃光のフレーム版。`tools.xrefs` で参照0件      | [閃光](../glint/README.md)                    |
| `0x100a5c5c`                               | 文字列               | `前方に合成`/`後方に合成`/`光成分のみ` の cp932 文字列3つ              | [blend_modes](blend_modes.md)                 |
| `0x100a6218` / `0x100a6340`                | `FILTER_DLL`         | `発光` オブジェクト / フレーム                                         | [発光](../luminous/README.md)                 |
| `0x100a6480` / `0x100a6488`                | `ex_data`            | `グロー` の `ex_data_def` / `+0x70` フィールド表                       | [グロー](../glow/README.md)                   |
| `0x100a6500`                               | `+0x74` 拡張ブロック | `グロー`。2つの登録エントリで共有                                      | [param_scaling](param_scaling.md)             |
| `0x100a6528` / `0x100a6650`                | `FILTER_DLL`         | `グロー` オブジェクト / フレーム                                       | [グロー](../glow/README.md)                   |
| `0x100a6790` / `0x100a67d4`                | 文字列               | `グロー` の `形状` 6項目 / 静的テキスト `形状`                         | [グロー](../glow/README.md)                   |
| `0x1005551c`                               | ジャンプテーブル     | `形状` の6分岐(`.text` 内)                                            | [グロー](../glow/README.md)                   |
| `0x1009d6c4`                               | 文字列               | `type`。コンボ選択値を指す `ex_data` フィールド名。**18個の表が共有**  | [filter_registration](filter_registration.md) |
| `0x100a722c`                               | `ex_data` フィールド表 | `ライト` の `+0x70`。`color`(3) + パディング(1) の1本だけ            | [ライト](../light/README.md)                  |
| `0x100a7228`                               | `ex_data_def`        | `ライト`。既定値 `ff ff ff ff`(白 + 未使用バイト、「指定なし」ではない) | [ライト](../light/README.md)                  |
| `0x100a7290`                               | `+0x74` 拡張ブロック | `ライト`。スケール `{10,1,10}`。`拡散` だけドラッグ範囲が表示範囲より狭い | [param_scaling](param_scaling.md)             |
| `0x100a72b0`                               | `FILTER_DLL`         | `ライト`(オブジェクトのみ)                                             | [ライト](../light/README.md)                  |
| `0x100a2d24`                               | 文字列               | `RGB ( %d , %d , %d )`。色選択ボタンのラベル書式。**14箇所が共有**(`発光`/`閃光`/`グロー`/`ライト`/`シャドー`/`縁取り` ほか) | [シャドー](../shadow/README.md)             |
| `0x100a0660` / `0x100a5e4c`                | 文字列               | パターン画像エラー2種(読み込み失敗 / サイズ超過)。**`シャドー` と `縁取り` が共有**(前者は `画像ファイル合成` も) | [シャドー](../shadow/README.md)               |
| `0x100a5ca8`                               | `ex_data` フィールド表 | `縁取り` の `+0x70`。`color`(3) + パディング(1) + `file`(256)。**`0x100b84b0`(シャドー)と中身が完全に同じ別テーブル** | [縁取り](../border/README.md)                 |
| `0x100a5cd8`                               | `+0x74` 拡張ブロック | `縁取り`。**4スロット中3つが NULL**、表示スケールも全ゼロ              | [param_scaling](param_scaling.md)             |
| `0x100a5cf8`                               | `FILTER_DLL`         | `縁取り`(オブジェクトのみ)。`flag = 0x40000020`                       | [縁取り](../border/README.md)                 |
| `0x1009d7c0` / `0x1009d7d0`                | 文字列               | `ex_data` フィールド名 `color` / `file`。**`シャドー` と `縁取り` が同じ2本を指す** | [縁取り](../border/README.md)             |
| `0x100a7d10`                               | `ex_data` フィールド表 | `ルミナンスキー` の `+0x70`。`type`(4) 1本だけ                       | [ルミナンスキー](../luma_key/README.md)       |
| `0x100a7d48`                               | `+0x74` 拡張ブロック | `ルミナンスキー`。スケール `{1,1}` / ドラッグ `0..4096` ×2            | [param_scaling](param_scaling.md)             |
| `0x100a7d60`                               | `FILTER_DLL`         | `ルミナンスキー`(オブジェクトのみ)                                     | [ルミナンスキー](../luma_key/README.md)       |
| `0x100a7e98`                               | 文字列               | `暗い部分を透過` から始まるコンボ4項目                                 | [ルミナンスキー](../luma_key/README.md)       |
| `0x100a8838` / `0x100a883c`                | `int[]`              | `モザイク` のスライダードラッグ下限 `{1}` / 上限 `{200}`               | [param_scaling](param_scaling.md)             |
| `0x100a8840`                               | `int[]`              | `モザイク` の表示スケール `{1}`                                        | [param_scaling](param_scaling.md)             |
| `0x100a8844`                               | 拡張ブロック         | `モザイク` の `+0x74` 本体(4本のポインタ)                              | [param_scaling](param_scaling.md)             |
| `0x100a8858` / `0x100a8980`                | `FILTER_DLL`         | `モザイク` オブジェクト / フレーム                                     | [モザイク](../mozaic/README.md)               |
| `0x100b84b0`                               | `ex_data` フィールド表 | `シャドー` の `+0x70`。`color`(3) + パディング(1) + **`file`(256、`kind=2`)** | [シャドー](../shadow/README.md)         |
| `0x100b8538`                               | `+0x74` 拡張ブロック | `シャドー`。スケール `{0,0,10,0}`、**グループ化マーカーはポインタごと NULL** | [param_scaling](param_scaling.md)         |
| `0x100b8560`                               | `FILTER_DLL`         | `シャドー`(オブジェクトのみ)。`flag = 0x40000020`                     | [シャドー](../shadow/README.md)               |
| `0x100b86e0` / `0x100b86d8`                | `int[2]`             | `シャープ` の表示スケール `{10, 1}` / ドラッグ上限 `{1000, 100}`      | [param_scaling](param_scaling.md)             |
| `0x100b86e8`                               | `+0x74` 拡張ブロック | `シャープ`。グループ化マーカーだけ NULL                                | [param_scaling](param_scaling.md)             |
| `0x100b86f8`                               | `FILTER_DLL`         | `シャープ`(オブジェクトのみ)。`ex_data_size = 0`、コールバック全 NULL | [シャープ](../sharp/README.md)                |
| `0x100a5680` / `0x100a57f8`                | `int[2]`             | `フェード` / `音量フェード` の表示スケール `{100, 100}`。中身の同じ別配列 | [param_scaling](param_scaling.md)         |
| `0x100a5688` / `0x100a5800`                | `+0x74` 拡張ブロック | 同上。**4スロット中、表示スケール以外すべて NULL**                     | [param_scaling](param_scaling.md)             |
| `0x100a5698` / `0x100a5810`                | `FILTER_DLL`         | `フェード`(`flag=0x20`)/ `音量フェード`(`flag=0x00200020`)。どちらも `ex_data_size = 0`、コールバック全 NULL | [フェード](../fade/README.md) |
| `0x100a747c`                               | `+0x74` 拡張ブロック | `斜めクリッピング`。スケール `{0,0,10,0,0}`(0 = 割らない)、**グループ化マーカーはポインタごと NULL**(`中心X`/`中心Y` を対にしていない) | [param_scaling](param_scaling.md)         |
| `0x100a7490`                               | `FILTER_DLL`         | `斜めクリッピング`(オブジェクトのみ)。`check_n = 0`、`ex_data_size = 0`、コールバック全 NULL | [斜めクリッピング](../diagonal_clipping/README.md) |
| `0x100a88cc`                               | ポインタ             | `モザイク` 登録構造体の `+0x74`                                        | [param_scaling](param_scaling.md)             |
| `0x100a989c` / `0x100a98a4` / `0x100a98ac` | 係数表               | RGB→YCbCr の Q14 固定小数点係数。BT.601 フルレンジと誤差0.3%以内       | [rgb_ycbcr](rgb_ycbcr.md)                     |
| `0x100b8c60`                               | ポインタ             | オフスクリーン描画 `0x1008a520` の呼び出し口                           | [canvas_growth](canvas_growth.md)             |

## 4. グローバル変数(実行時に書かれる状態)

`ルミナンスキー` と `音量フェード` はこの表に1行も持たない ―― **専用グローバルを
1つも使わない2つのエフェクト**である
([`filter_registration.md` §6](filter_registration.md))。理由は別々で、
`ルミナンスキー` はワーカーが `fp->track[]` を自分で読み、`音量フェード` は
そもそもワーカーを起動しない。逆に `フェード` がグローバルを1個だけ持つのは、
`exec_multi_thread_func` が `fp`/`fpip` 以外を渡せないからである。

| アドレス                                   | 中身                                                                       | 所有       | 詳細                                   |
| ------------------------------------------ | -------------------------------------------------------------------------- | ---------- | -------------------------------------- |
| `0x1011ec34` / `0x1011ec38`                | 垂直 / 水平パスの半径                                                      | `ぼかし`   | [ぼかし](../blur/README.md)            |
| `0x1011ec3c` / `0x1011ec40`                | 同・カーネル幅 (`2r+1`)                                                    | `ぼかし`   | [ぼかし](../blur/README.md)            |
| `0x1011ec7c` / `0x1011ec80`                | 16bit マット map A / map B(どちらも `*(fpip+0xB0)` の中)                  | `クロマキー` | [クロマキー](../chroma_key/README.md)  |
| `0x1011ec84`                               | 同・map C(`0x101a5328` を借りる。`色彩補正` のときだけ)                   | `クロマキー` | [クロマキー](../chroma_key/README.md)  |
| `0x1011ec88` / `0x1011ec8c`                | カーネル幅 `2r+1` / `境界補正` の半径 `r`                                 | `クロマキー` | [クロマキー](../chroma_key/README.md)  |
| `0x1011ec90`                               | `クロマキー` の `ex_data_def`。.data にあるが**誰も書かない**12バイトのゼロ | `クロマキー` | [クロマキー](../chroma_key/README.md)  |
| `0x1011ed34` / `0x1011ed38`                | カーネル幅 `2r+1` / `境界補正` の半径 `r`。**これが全グローバル**         | `カラーキー` | [カラーキー](../color_key/README.md)   |
| `0x1011ed3c`                               | `カラーキー` の `ex_data_def`。同じく誰も書かない12バイトのゼロ           | `カラーキー` | [カラーキー](../color_key/README.md)   |
| `0x1011ed48` / `0x1011ed54`                | `カラーキー` の `track_default[3]` / `track_s[3]`。全ゼロなので .bss 送り | `カラーキー` | [カラーキー](../color_key/README.md)   |
| `0x100d7588`                               | `scale = 高さ_raw / (200 * steps)`(double)。**唯一の丸め誤差の出どころ**  | `凸エッジ` | [凸エッジ](../convex_edge/README.md)   |
| `0x100d7590`                               | `steps`(= クランプ後の `幅`、描画品質オンなら16)                         | `凸エッジ` | [凸エッジ](../convex_edge/README.md)   |
| `0x100d7594` / `0x100d7598`                | 方向ベクトル `dy = trunc(cos(角度)*65536)` / `dx = trunc(-sin(角度)*65536)` | `凸エッジ` | [angle_vector](angle_vector.md)        |
| `0x1011efdc`                               | `強さ` から作った Q12 の不透明度                                           | `拡散光`   | [拡散光](../diffusion_light/README.md) |
| `0x1011efe0` / `0x1011efe4`                | カーネル幅 / 現ラウンドの半径                                              | `拡散光`   | [拡散光](../diffusion_light/README.md) |
| `0x10134e6c` / `0x10134e74`                | `しきい値`(符号付き)/ `強さ`。どちらも Q12                               | `エッジ抽出` | [エッジ抽出](../edge_extraction/README.md) |
| `0x10134e70` / `0x10134e72` / `0x10134e78` | 光色の Cb / Cr / Y(word)。出力画素にそのまま書かれる                     | `エッジ抽出` | [エッジ抽出](../edge_extraction/README.md) |
| `0x10135c68` / `0x10149840`                | 輝度カーブヘルパーの行幅[画素](8B / 6B)                                    | 共有       | [exp_log_curve](exp_log_curve.md)      |
| `0x10178ec0` / `0x101790c8`                | **最大フレーム幅 / 高**。矩形転送・矩形クリアが6バイト画素で選ぶ側         | 共有       | [canvas_growth](canvas_growth.md)      |
| `0x101920e0`                               | オブジェクトキャンバスの**最大高**                                         | 共有       | [canvas_growth](canvas_growth.md)      |
| `0x10196748`                               | 同・**最大幅**                                                             | 共有       | [canvas_growth](canvas_growth.md)      |
| `0x101a5328`                               | ぼかし中間のスクラッチバッファ。`グロー` と `クロマキー` も同じ番地を借りる | `発光`     | [発光](../luminous/README.md)          |
| `0x101b2010`〜`0x101b203c`                 | 毎フレーム状態ブロック(w/h・強さ・半径3種・光色・バッファ2本・しきい値)   | `グロー`   | [グロー](../glow/README.md)            |
| `0x101a6b7c`〜`0x101a6bac`                 | 毎フレーム状態ブロック(中心座標・しきい値・光色)                           | `閃光`     | [閃光](../glint/README.md)             |
| `0x101a6b90` / `0x101a6b9c` / `0x101a6bac` | 光色の Cb / Cr / Y                                                         | `閃光`     | [rgb_ycbcr](rgb_ycbcr.md)              |
| `0x101a6b94`                               | `strength`。読む命令は `func_proc` 内の1箇所だけ                           | `閃光`     | [閃光](../glint/README.md)             |
| `0x101b1ff4` / `0x101b1ffc` / `0x101b2004` | 光色の Cb / Cr / Y                                                         | `発光`     | [rgb_ycbcr](rgb_ycbcr.md)              |
| `0x101b20ac`                               | スクラッチバッファ ポインタ = `[0x101a5328]` を再度借用                    | `ライト`   | [ライト](../light/README.md)           |
| `0x101b20b0`                               | 実行中のパスの係数(後光=A、陰影=Bまたは`trunc(B*2/11)`)。両パスが排他的に使う | `ライト`   | [ライト](../light/README.md)           |
| `0x101b20b4` / `0x101b20b8`                | カーネル幅 `2r+1` / 半径 `r`(2段クランプの結果、パスの間で上書きされる)    | `ライト`   | [ライト](../light/README.md)           |
| `0x101b20bc` / `0x101b20be` / `0x101b20c0` | 光色の Cb / Cr / Y                                                         | `ライト`   | [rgb_ycbcr](rgb_ycbcr.md)              |
| `0x101b1e30` / `0x101b1e34`                | 16bit 被覆マップのプレーン = `[0x101a5328]` / カーネル幅 `2*サイズ+1`      | `縁取り`   | [縁取り](../border/README.md)          |
| `0x101b1e38`                               | `4096 - trunc(ぼかし*4096/1000)`。**書かれるだけで読み手が1つも無い**      | `縁取り`   | [縁取り](../border/README.md)          |
| `0x101b1e3c` / `0x101b1e40`                | 内容ボックスの原点 x / y。**常に 0**、参照側(4本の矩形クリア)ごと死んでいる | `縁取り`  | [縁取り](../border/README.md)          |
| `0x101b1e48`                               | `縁取り` の `ex_data_def`。260バイトのゼロ                                 | `縁取り`   | [縁取り](../border/README.md)          |
| `0x101b1f4c` / `0x101b1f4e` / `0x101b1f50` | 縁色の Cb / Cr / Y                                                         | `縁取り`   | [rgb_ycbcr](rgb_ycbcr.md)              |
| `0x101b1f54`                               | `gain = round(1024/(0.02*サイズ*ぼかし+1))`。窓の和に**2パスとも**掛かる   | `縁取り`   | [縁取り](../border/README.md)          |
| `0x101b1f58` / `0x101b1f60`                | `track_s[2]` / `+0x74` の表示スケール配列。どちらも全ゼロなので .data 送り | `縁取り`   | [param_scaling](param_scaling.md)      |
| `0x10231f90` / `0x10231fa0`                | 16bit アルファのスクラッチ プレーン1 / 2(`[0x101a5328]` とその後ろ半分)  | `シャドー` | [シャドー](../shadow/README.md)        |
| `0x10231f94` / `0x10231f98`                | カーネル幅 B(`2*ceil(拡散/2)+1`)/ A(`2*floor(拡散/2)+1`)               | `シャドー` | [シャドー](../shadow/README.md)        |
| `0x10231f9c`                               | `density = trunc(濃さ*4096/1000)`                                          | `シャドー` | [シャドー](../shadow/README.md)        |
| `0x10231fa4` / `0x10231fa8`                | 影の箱の原点 `max(X,0)` / `max(Y,0)`                                       | `シャドー` | [シャドー](../shadow/README.md)        |
| `0x10231fb0`                               | `シャドー` の `ex_data_def`。260バイトのゼロ                               | `シャドー` | [シャドー](../shadow/README.md)        |
| `0x102320b4` / `0x102320b6` / `0x102320b8` | 影色の Cb / Cr / Y                                                         | `シャドー` | [rgb_ycbcr](rgb_ycbcr.md)              |
| `0x102320bc` / `0x102320c0`                | 垂直 / 水平パスの半径。`ぼかし` の `0x1011ec34`/`38` に対応する2本         | `シャープ` | [シャープ](../sharp/README.md)         |
| `0x102320c4` / `0x102320c8`                | 同・カーネル幅 `2r+1`。`強さ` 用のグローバルは**無い**(ワーカーが `fp->track[0]` を直接読む) | `シャープ` | [シャープ](../sharp/README.md) |
| `0x102320cc` / `0x102320d4`                | `シャープ` の `track_s[2]` / `+0x74` のドラッグ下限。全ゼロなので `.data` 送り | `シャープ` | [param_scaling](param_scaling.md)   |
| `0x101a5390`                               | Q12 のアルファ。`func_proc` が書きワーカーが読む。**参照は6箇所すべてフェード内** | `フェード` | [フェード](../fade/README.md)   |
| `0x101b20c4`                               | `hw = 幅 << 15`。Q16 での「半値幅」で、符号が3モードの2つ目/3つ目を選ぶ    | `斜めクリッピング` | [斜めクリッピング](../diagonal_clipping/README.md) |
| `0x101b20c8` / `0x101b20cc`                | 直線が通る点 `cx = w/2 + 中心X` / `cy = h/2 + 中心Y` [画素]                | `斜めクリッピング` | [斜めクリッピング](../diagonal_clipping/README.md) |
| `0x101b20d0` / `0x101b20d4`                | 法線 `vx = trunc(sin(θ)·65536)` / `vy = trunc(cos(θ)·(-65536))`          | `斜めクリッピング` | [angle_vector](angle_vector.md) |
| `0x101b20d8`                               | `ぼかし + 1`。**必ず +1 されるので階調帯は最低1画素**                      | `斜めクリッピング` | [斜めクリッピング](../diagonal_clipping/README.md) |
| `0x101bad24` / `0x101bad28` / `0x101bad2c` | `size` / `offset_x` / `offset_y`                                           | `モザイク` | [モザイク](../mozaic/README.md)        |
| `0x101daf68`                               | 三角関数テーブル(cos 系、**式は未検算**)                                   | 共有       | [lut_tables](lut_tables.md)            |
| `0x101dcf78`                               | イージングテーブル `trunc(2048*(1-cos(i*pi/4096)))`、4097要素              | 共有       | [lut_tables](lut_tables.md)            |
| `0x101def78`                               | 上記の末尾要素。逆引き `*(0x101def78 - 2*t)` の基点                        | 共有       | [lut_tables](lut_tables.md)            |
| `0x101def80`                               | 三角関数テーブル(sin 系、**式は未検算**)                                   | 共有       | [lut_tables](lut_tables.md)            |
| `0x101e42f4` / `0x101e4300` / `0x101e4304` | 輝度カーブヘルパーへの引数(画像 / w / h)                                   | 共有       | [exp_log_curve](exp_log_curve.md)      |
| `0x101e4310` / `0x101e4318`                | `1/ln(base)` / `base`                                                      | 共有       | [exp_log_curve](exp_log_curve.md)      |
| `0x101e9edc` 系                            | `table[0x44]` の代替クリップ矩形(bit2)。**未確定**                         | 共有       | [blend_modes](blend_modes.md)          |
| `0x101ec840` 系                            | 同(bit4)。**未確定**                                                       | 共有       | [blend_modes](blend_modes.md)          |
| `0x102308e0` / `0x10230918`                | `table[0x44]` の転送元 / 転送先の画素バイト数(8 or 6)                      | 共有       | [blend_modes](blend_modes.md)          |
| `0x10230900`                               | `table[0x44]` の不透明度(`0x1000 - 第9引数`)。0 以外を渡す呼び出し元が無い | 共有       | [blend_modes](blend_modes.md)          |

## 5. まだ埋まっていないところ

各エフェクト README の「未確定事項」から、**アドレスに紐付くもの**だけを抜粋。

| 対象                  | 分かっていないこと                                                                                      |
| --------------------- | ------------------------------------------------------------------------------------------------------- |
| `0x101daf68` / `0x101def80` | 生成式(cos 系 / sin 系という分類のみ)と参照元。`ノイズ.func_init` が書く以外の絶対 dword 参照は image 中に無い([`lut_tables.md` §5](lut_tables.md)) |
| `0x1008a520`          | 中身。`発光` からは呼び出しだけを確認                                                                   |
| `0x1009fbb0`          | 32エントリ中29個。`0x01`〜`0x0c` が AviUtl の合成モード一覧に対応していそうだが未検証                   |
| `0x10081b40`          | 下位バイト bit2 / bit4 が選ぶ代替クリップ矩形の意味。第9引数(不透明度)の実挙動                          |
| `0x10081f90`          | フラグ `2` の bit1 以外のビット                                                                         |
| `0x10135c68`          | **書き込み側**。`*(fpip+0xEC)` と同値であることは前提にしている                                         |
| `0x101539c0`          | `table[0x44]` が8バイト画素のときに使う行幅。`シャープ` はここが `*(fpip+0xEC)` と同値である前提でスクラッチを索いている。**書き込み側は未追跡**(`0x10135c68` と同じ形の宿題) |
| `jge / test tid / jne` のガード | 差し替え `thread_num = 1` を持たない40箇所以上について、`寸法 < thread_num` で何が起きるか。確定しているのは `シャープ` の3ワーカーだけ([`thread_split.md`](thread_split.md)) |
| `0x10178ec0` / `0x101790c8` | **書き込み側**。最大フレームサイズという読みは、矩形クリアの画素サイズ分岐と `グロー` の使い方から |
| `0x100136e0` / `0x10013880` | `境界補正` の3パスが `2r+1` 未満の画像で枠外へ書く先。`r ≤ 5` なので幅か高さが11未満のときだけ    |
| `fp+0xE4`             | `クロマキー` がスポイト起動 `table[0x68]` に、`発光`/`グロー`/`ライト` が `table[0x6c]`/`table[0x80]` に渡す値。exedit 側の何を指すかは未追跡 |
| `table[0x4c]` の `kind` | `5`(ボタン)/`6`(コンボ)/`7`(静的テキスト)は確定。**`0`〜`4` が選ぶ種別は未追跡**                     |
| `0x1004a7e0`          | `(fp+0xE4, フィールド名文字列)` を引数に取る。`発光`/`グロー`/`ライト` が色選択ボタン確定後に ex_data のフィールド名(`"color"`/`"no_color"`)を渡して呼ぶが、中身(アンドゥ履歴? dirty フラグ?)は未読 |
| `table[0x6c]`(`0x1004d2a0`) | `"GET_COLOR"` を `sub_10020800` へ渡す色選択ダイアログの起動口だと読める。フラグ引数 bit8 は「指定なし」トグルの有無らしいが、bit1/bit9/bit10 や `sub_10020800` 自体は未追跡 |
| `table[0x80]`(`0x1004dac0`) | 汎用コンボ `func_WndProc` の選択変更時、および `発光`/`グロー`/`ライト` の色選択ボタンの先頭で共通に呼ばれる。中身は `sub_1008d150()` → 引数を `-1` して `sub_1008d290` へ末尾呼び出し、だが戻り値はどこでも捨てられておりその2関数の意味は未読 |
| `0x1004b200`          | `シャドー` の `影を別オブジェクトで描画` が呼ぶ描画パイプラインの入口。第3引数 `0x10fffff` のビットの意味も、`0x1002b0d0`/`0x10049370`/`0x10039550` へ分岐する先も未追跡 |
| `0x101b1e38`          | `縁取り` が毎フレーム書いて**誰も読まない** `4096 - trunc(ぼかし*4096/1000)`。被覆マップのしきい値か不透明度として使うつもりだったように見えるが、裏付けは無い([`縁取り` §10](../border/README.md)) |
| `0x101b1e3c` / `0x101b1e40` | `縁取り` の内容ボックス原点。0 以外を書く命令が image 中に存在せず、参照側の4本の矩形クリアごと到達不能。`シャドー` の `g_X`/`g_Y` からの写しだと読めるが、そう断定する材料は無い |
| `0x1004c8d0` / `0x1004cc40` | `table[0x38]`(画像ローダ)がサポートする形式、`table[0x3c]` が組み立てる相対パスの基準              |
| `flag` bit30(`0x40000000`) | SDK では `FILTER_FLAG_MAIN_MESSAGE`。exedit の内部テーブルでは `標準描画`/`拡張描画`/`パーティクル出力`/`シャドー`/`縁取り`/`部分フィルタ`/`グループ制御`/`カメラ制御`/`放射ブラー`・`方向ブラー` の2つめ、の12エントリに立っているが共通項が不明 |
| x87 の精度制御          | 53bit(MSVC の既定)か 64bit か。`シャドー` の「除算を先に丸めてから掛ける」出力で 1 単位の差になる([`シャドー` §5](../shadow/README.md)) |
| x87 の `fsin` / `fcos`  | libm と1 ulp 違ううるので、`sin(角度)*65536` が整数のすぐ近くに落ちる **(角度, 成分) の組**(すべて30度の倍数。生値 ±3600 で34個、±36000 で322個)で Q16 成分が 1 ずれうる([`angle_vector.md` §5](angle_vector.md)) |
| `0x1009a410` の他の利用者 | `方向ブラー`/`フレームバッファ`/`グラデーション` が同じ命令列で Q16 の2値を作るが、**どちらの成分を x に使うか**はワーカーを読んでいない。`凸エッジ` と `斜めクリッピング` については確定済み([`angle_vector.md` §3/§6](angle_vector.md)) |
| `fp+0xE4`             | `クロマキー` がスポイト起動 `table[0x68]` に渡す値。`エッジ抽出` も色ダイアログの前後で同じ値を渡す。exedit 側の何を指すかは未追跡 |
| `[fp+0x64]+0x6c`      | 色ダイアログの第3引数。bit `0x100` = 「指定なし」ボタンまでは確定。`拡張色設定` が使う `0x202`/`0x402` は未追跡 |
| `[fp+0x64]+0x80` / `0x1004a7e0` | 色ダイアログの前後で必ず呼ばれる2つ。undo の開始とフィールドの dirty マークと推測しているだけ |
| `fpip+0xFC` / `+0x100` | **書き込み側**。フレームレートの分子/分母であることは2方向の使われ方から確定しているが、どこで埋まるかは未追跡([`object_time.md` §4](object_time.md)) |
| `fpip+0x118`          | 通常のオブジェクトでの中身。変える書き込みは `テキスト` の `0x1008b2f2` だけで、読み手はいずれも 0 前提([`object_time.md` §5](object_time.md)) |
| `fpip+0x12C`          | `時間制御` の時刻表を書く側。`時間制御` の `func_proc`(`0x10007de0`)は `xor eax,eax; ret` の2命令しかない |
| `0x1004993e` / `0x10049fab` | `func_proc` が 0 を返したあと。前者は関数ごと `return` することまで確認したが、後者は内側ループを抜けるだけでその先は未追跡 |
| `fpip->flag & 0x200`  | exedit 側でいつ立つか。読み手は一様に「サンプル数を削ってよい」と解釈している                           |
| `fpip+0xD4` / `+0xD8` | 単位。`閃光` の補正式から 1/4096 画素と推定しているだけ                                                 |
| `aviutl.exe` 側       | 最終的な YCbCr→RGB 変換とクリップ。満輝度超過や色差の折り返しがどう出るかは全エフェクトで推定にとどまる |

コードに紐付かない未確定事項(`拡散光` の `2.236` の由来、`モザイク` の
`h < thread_num` での重複処理の実害、など)は各 README の末尾を参照。
