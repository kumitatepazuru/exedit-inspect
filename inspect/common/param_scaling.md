# トラックバーの生値・表示スケール・定数除算

UI に出ている数値と `fp->track[]` の生値、そして生値から内部の固定小数への
変換。**この対応は `func_proc` を読んでも出てこない** ―― どのエフェクトも
`fp->track[]` を生値のまま使うだけで、表示の整形には関与していないからである。
対応はコードではなく**テーブル**に入っている。

## 1. `+0x74` 拡張ブロック

`0x100a3e28` の配列が指す登録構造体は `data/filter.h` の `FILTER` より長く、
`+0x74`(SDK では `func_is_saveframe`)に exedit 独自ブロックへのポインタがある。
ブロックは `int*` 4本:

```
struct +0x74 -> {
    int *scale;       // [0] 表示スケール(生値をこれで割って表示)
    int *group;       // [1] 多成分コントロールのグループ化マーカー(単独なら NULL/0)
    int *drag_min;    // [2] スライダーのドラッグ下限
    int *drag_max;    // [3] スライダーのドラッグ上限
}
```

いずれも `int[track_n]`。実例(`モザイク`):

```
0x100a88cc (struct + 0x74) -> 0x100a8844
0x100a8844: 0x100a8840 -> int[1] = {  1}      表示スケール
            0x00000000                        グループ化(単独トラックバーなので NULL)
            0x100a8838 -> int[1] = {  1}      ドラッグ下限
            0x100a883c -> int[1] = {200}      ドラッグ上限
```

```
uv run main.py tools.track_scale                 # 全エフェクト
uv run main.py tools.track_scale --name <名前>   # 1エフェクト分
uv run main.py tools.track_scale --groups        # slot[1] の根拠を全件出力
```

スロット `[0]` がスケールであることは推測ではなく、**表示値が既知のフィルタで
検算できる** ―― `透明度`(スケール10、生値 0~1000 → 0.0~100.0)、
`拡大率`(スケール100、初期値10000 → 100.00)、`発光` の `強さ`(スケール10、
初期値1000 → 100.0)。いずれも一致する。

### 解析済みエフェクトの一覧

| エフェクト                                 | トラックバー | スケール | UI表示範囲   | ドラッグ範囲 | 初期値(生値) |
| ------------------------------------------ | ------------ | -------- | ------------ | ------------ | ------------ |
| [`ぼかし`](../blur/README.md)              | `範囲`       | 1        | 0~1000       | 0~300        | 5            |
|                                            | `縦横比`     | 10       | -100.0~100.0 | -100.0~100.0 | 0            |
|                                            | `光の強さ`   | 1        | 0~60         | 0~60         | 0            |
| [`境界ぼかし`](../boundary_blur/README.md) | `範囲`       | 1        | 0~2000       | 0~500        | 5            |
|                                            | `縦横比`     | 10       | -100.0~100.0 | -100.0~100.0 | 0            |
| [`発光`](../luminous/README.md)            | `強さ`       | 10       | 0.0~200.0    | 0.0~200.0    | 1000         |
|                                            | `拡散`       | 1        | 10~2000      | 10~800       | 250          |
|                                            | `しきい値`   | 10       | 0.0~200.0    | 0.0~200.0    | 800          |
|                                            | `拡散速度`   | 1        | 0~60         | 0~60         | 0            |
| [`モザイク`](../mozaic/README.md)          | `サイズ`     | 1        | 1~2000       | 1~200        | 12           |
| [`閃光`](../glint/README.md)               | `強さ`       | 10       | 0.0~100.0    | 0.0~100.0    | 1000         |
|                                            | `X` / `Y`    | 1        | -2000~2000   | -1000~1000   | 0 / 0        |
| [`拡散光`](../diffusion_light/README.md)   | `強さ`       | 10       | 0.0~100.0    | 0.0~100.0    | 500          |
|                                            | `拡散`       | 1        | 0~500        | 0~100        | 12           |
| [`グロー`](../glow/README.md)              | `強さ`       | 10       | 0.0~400.0    | 0.0~100.0    | 400          |
|                                            | `拡散`       | 1        | 0~200        | 0~100        | 30           |
|                                            | `しきい値`   | 10       | 0.0~200.0    | 0.0~100.0    | 400          |
|                                            | `ぼかし`     | 1        | 0~50         | 1~10         | 1            |
| [`クロマキー`](../chroma_key/README.md)    | `色相範囲`   | ―(1)    | 0~256        | ―           | 24           |
|                                            | `彩度範囲`   | ―(1)    | 0~256        | ―           | 96           |
|                                            | `境界補正`   | ―(1)    | 0~5          | ―           | 1            |
| [`カラーキー`](../color_key/README.md)     | `輝度範囲`   | ―(1)    | 0~4096       | ―           | **0**        |
|                                            | `色差範囲`   | ―(1)    | 0~4096       | ―           | **0**        |
|                                            | `境界補正`   | ―(1)    | 0~5          | ―           | **0**        |
| [`ルミナンスキー`](../luma_key/README.md)  | `基準輝度`   | 1        | -4096~8192   | 0~4096       | 2048         |
|                                            | `ぼかし`     | 1        | 0~4096       | 0~4096       | 512          |
| [`ライト`](../light/README.md)             | `強さ`       | 10       | 0.0~300.0    | 0.0~300.0    | 1000         |
|                                            | `拡散`       | 1        | 0~500        | **0~100**    | 25           |
|                                            | `比率`       | 10       | -100.0~100.0 | -100.0~100.0 | 0            |
| [`シャドー`](../shadow/README.md)          | `X`          | **0**(=1) | -1000~1000  | **-200~200** | -40          |
|                                            | `Y`          | **0**(=1) | -1000~1000  | **-200~200** | 24           |
|                                            | `濃さ`       | 10       | 0.0~100.0    | 0.0~100.0    | 400          |
|                                            | `拡散`       | **0**(=1) | 0~500       | **0~50**     | 10           |

**ドラッグ範囲が UI 表示範囲より狭いトラックバーは、その先を数値入力でしか
指定できない**(`ぼかし` の `範囲` 301~1000 など)。`ルミナンスキー` の
`基準輝度` は**下限側もはみ出している**唯一の例で、ドラッグで届く 0~4096 は
ちょうど PIXEL_YC の輝度の表現可能域にあたる。届かない外側はどちらも
「何もしない」か「全部消す」に退化する([`ルミナンスキー` §6](../luma_key/README.md))。

`クロマキー` と `カラーキー` は `+0x74` そのものが **NULL** で、スケールも
ドラッグ範囲もグループ化マーカーも持たない。ブロックが無いときは AviUtl 側の既定
(スケール1、ドラッグ範囲はトラックバーの範囲そのもの)になる。

`シャドー` のスケール配列は `{0, 0, 10, 0}` で、**スケールしないトラックバーに
1 ではなく 0 が入っている**唯一の例。AviUtl 側はどちらも「割らない」として
扱うので表示は同じで、`tools.track_scale` も 0 を 1 に読み替えている。

`カラーキー` は**3本とも初期値が 0** で、スポイトでキー色を取った直後でも
「キー色と完全に一致する画素だけを消す」状態から始まる。解析済みの中で
**初期状態が実質何もしないトラックバー構成**はこれだけである。

### slot[1] ―― グループ化マーカー

この枠が非ゼロなのは**多成分コントロールを構成するトラックバーだけ**で、必ず
「最後以外は `+1`、最後は `-(要素数-1)`」の形になる。

| 例                                            | 値               |
| --------------------------------------------- | ---------------- |
| `閃光` の `X`, `Y`                            | `+1`, `-1`       |
| `X`, `Y`, `Z` / `拡張色設定` の `R`, `G`, `B` | `+1`, `+1`, `-2` |
| 単独トラックバー                              | 常に 0           |

`モザイク` の解析時点では用途未確定だったが、`閃光` の `X`/`Y` で初めて非ゼロが
出て確定した(`tools.track_scale --groups` が全件の根拠を出す)。

**「対に見える」ことと「グループである」ことは別**である。`シャドー` の
`X`/`Y` はスロット[1] のポインタ自体が NULL で、`閃光` の `X`/`Y`(`+1, -1`)
とは扱いが違う ―― 同じ名前・同じ意味の2本でも、登録側がグループにするか
どうかは揃っていない([`シャドー` §1](../shadow/README.md))。

## 2. マジックナンバー除算 `0x10624dd3`

生値から内部固定小数への変換は、**全エフェクトが同じ MSVC の定型命令列**を使う。
`0x10624dd3` を掛けて上位32bitを取り、`sar 6` する ―― 実質 `>>38` ―― に符号補正を
足したもので、意味は `/1000` である。

```
fixed = trunc(raw * 4096 / 1000)      // 強さ・しきい値の類
r     = trunc(範囲 * (1000 ± 縦横比) / 1000)   // 半径の類
```

**四捨五入ではなく0方向への切り捨て**であることが要点で、リファレンス実装を
Python で書くときに `//` を使うと負の入力で食い違う([`integer_semantics.md`](integer_semantics.md))。

| エフェクト   | 適用先                                | 検算                                     |
| ------------ | ------------------------------------- | ---------------------------------------- |
| `ぼかし`     | `範囲`×`縦横比` → 半径                | 全2,002,000通りで `trunc(x/1000)` と一致 |
| `境界ぼかし` | 同上(式まで完全に同一)                | 400万通り全数一致                        |
| `発光`       | `強さ` / `しきい値` → `raw*4096/1000` | raw=0~2000 の全2001通り                  |
| `閃光`       | `強さ` → `raw*4096/1000`              | 全1001通り                               |
| `拡散光`     | `強さ` → `raw*4096/1000`              | 全1001通り                               |
| `グロー`     | **`しきい値` だけ** → `raw*4096/1000` | 全2001通り                               |
| `ライト`     | `強さ` → `raw*4096/1000`。`比率` も同じ除算を経由して `強さ` 由来の係数を2本(A・B)に分ける | 172,029通りの `(強さ,比率)` 組で一致([`ライト` §2](../light/README.md)) |
| `シャドー`   | **`濃さ` だけ** → `raw*4096/1000`。`X`/`Y`/`拡散` は生の画素数 | 全1001通り。エフェクト全域に `0x10624dd3` は1個だけ([`シャドー` §2](../shadow/README.md)) |
| `クロマキー` | **無し**。3本とも生値のまま `<<7` / `*key_sat>>8` / 半径 | `func_proc` にもワーカーにも `0x10624dd3` が1つも無い |
| `カラーキー` | **無し**。3本とも生値のまま区間の半幅 / 半径          | エフェクト全域(1,844バイト)に `0x10624dd3` も x87 も0件 |
| `ルミナンスキー` | **無し**。2本とも生値のまま帯の中心 / 半幅        | エフェクト全域(959バイト)に `0x10624dd3` も x87 も0件   |

**`グロー` の `強さ` はこの変換を受けない**。生値のまま保持され、以降は小さい
整数倍・整数除算しか受けない唯一の例外である(`func_proc` 全体を走査しても
`0x10624dd3` は `しきい値` 用の1個しか無い ―― [`グロー` §2](../glow/README.md))。

`4096` が満輝度(= UI の 100.0%)にあたるので、「`raw * 4096 / 1000`」は
**表示スケール10のトラックバーで 100.0 のとき Q12 の 1.0** という意味になる。

`閃光` にはもう1箇所、`*1000 / 250`(`sar edx, 4`)の形で現れる**単なる4倍**が
ある。キャンバス拡張量の計算で、-500k~500k の全域で `*4` と一致することを
確認済み([`閃光` §3](../glint/README.md))。

`ライト` にはこの2つとは別のマジックナンバー `0x2e8ba2e9`(shift 1、`/11`)が
1箇所だけ現れる ―― `逆光` チェックボックスがオフ(既定)のときだけ、陰影パスの
係数を `trunc(B*2/11)` に縮める(`0x10624dd3` の `/1000` とは無関係な独立の
定数、[`ライト` §3](../light/README.md))。

## 3. `raw == 0` の扱い

先頭トラックバーが 0 のときの挙動はエフェクトごとに違う。

| エフェクト   | 条件         | 動作                                                   |
| ------------ | ------------ | ------------------------------------------------------ |
| `ぼかし`     | `範囲 == 0`  | 即 `return`。画像に一切触れない                        |
| `閃光`       | `強さ == 0`  | 即 `return`。キャンバスも広がらない                    |
| `拡散光`     | `強さ == 0`  | 即 `return TRUE`                                       |
| `モザイク`   | `サイズ < 2` | 即 `return TRUE`(1px ブロックは恒等なので結果は正しい) |
| `境界ぼかし` | `範囲 == 0`  | **即 return しない**。`rx=ry=0` のままワーカーへ進む   |
| `グロー`     | `強さ == 0` **または** `拡散 == 0` | 即 `return 1`。**2つのトラックバーが独立に早期 return を持つ**唯一の例 |
| `クロマキー` | (トラックバーではなく) `ex_data.status != 1` | 即 `return 1`。**早期 return の条件がトラックバーでない**。`色相範囲 = 0` でも `境界補正 = 0` でもワーカーは走る |
| `カラーキー` | 同上 `ex_data.status != 1`                   | 同上。`輝度範囲 = 色差範囲 = 0` でもキーワーカーは走る(1点だけのキーになる) |
| `ルミナンスキー` | **無し**                                 | `func_proc` は `fp->track[]` を1度も読まない。`ぼかし = 0` でも必ず全画素を走査し、そのときは4モードともハードステップになる(ゼロ除算にはならない) |
| `シャドー`   | `濃さ == 0`  | 即 `return 1`。**先頭ではなく3本目**のトラックバーが早期 return を持つ唯一の例。`拡散 = 0` はカーネル幅1(恒等)になるだけで通り抜ける |

## 4. クランプの有無

| エフェクト | パラメータ | 上限                                                                                                          |
| ---------- | ---------- | ------------------------------------------------------------------------------------------------------------- |
| `発光`     | `強さ`     | 4096 でクランプ。**超過分は別枠の定数オフセット**として保持され、抽出時に加算される(100%超のオーバードライブ) |
| `発光`     | `しきい値` | クランプなし(raw=2000 で 8192。どの画素も超えられなくなるだけ)                                                |
| `拡散光`   | `強さ`     | raw の上限が1000なので 4096 を超えられない。オーバードライブ経路は**コード上に存在しない**                    |
| `閃光`     | `強さ`     | 同上。`T = max(0, 4096 - strength)` として使われる                                                            |
| `グロー`   | `強さ`     | クランプ以前に固定小数ですらない。生値がそのまま `(sum >> 4) * strength >> 10` の係数になる([`グロー` §4](../glow/README.md)) |
| `グロー`   | `しきい値` | クランプなし(raw=2000 で 8192)。`発光` と同じ                                                |
| `ルミナンスキー` | `基準輝度` / `ぼかし` | **どちらもクランプなし**。生値がそのまま `PIXEL_YC.y` と比較されるので、帯が表現可能域(0~4096)の外へ出た瞬間に無効化 / 全消しへ退化する                                                                |
| `シャドー`   | `濃さ`     | raw の上限が1000なので 4096 を超えられない。出力アルファも構造的に `density` 止まりで、**クランプ命令そのものが無い**([`シャドー` §5](../shadow/README.md)) |
